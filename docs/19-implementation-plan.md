# 19 — Implementation plan & build log (autonomous build)

> **STATUS: CURRENT — orchestration anchor.** Spec/de-risk done ([18 §0]: Phase 0 PASS trên 2 máy thật). Đây là bản đồ **build → workflow** + trạng thái, cập nhật sau mỗi workflow. Triết lý: **codebase đẹp nhất, kiến trúc tốt nhất, không fallback** (libghostty-only, no SwiftTerm).

## Tên sản phẩm & module

Product/codename: **Aislopdesk** (remote workspace — khớp repo `aislopdesk`). Module prefix `Aislopdesk`.

```
aislopdesk/
├── Package.swift                 # SPM: libs + CLI execs + tests (HEADLESS-buildable & testable)
├── Sources/
│   ├── AislopdeskProtocol/            # wire format: framing, MessageType, seq(int64), Hello/Ack — pure Swift, 0 platform dep
│   ├── AislopdeskTransport/           # NWConnection + TCP_NODELAY, dual data/control channel,
│   │                             #   BackedWriter/BackedReader (ET replay 64MB/4MB gate), reconnect handshake
│   ├── AislopdeskHost/                # macOS: PTY (openpty + posix_spawn createSession), session mgr,
│   │                             #   no-buffer PTY↔transport relay (QoS user-interactive), TIOCSWINSZ
│   ├── AislopdeskClient/              # shared client: connection mgr, reconnect, input encoding
│   └── AislopdeskTerminal/            # TerminalSurface protocol + HeadlessSurface (libghostty impl = GUI app target)
│   ├── aislopdesk-hostd/              # exec: headless host daemon (PTY + transport)  ← buildable tonight
│   └── aislopdesk-client/             # exec: headless CLI test client                ← buildable tonight
├── Tests/{AislopdeskProtocol,AislopdeskTransport,AislopdeskHost,AislopdeskClient}Tests/
├── Apps/                         # Xcode projects (GUI; depend on SPM + libghostty XCFramework) — scaffold
│   ├── HostApp-macOS/  ClientApp-macOS/  ClientApp-iOS/
├── ThirdParty/ghostty/           # build script + External.zig patch → libghostty.xcframework
└── docs/
```

**Why headless-first:** the whole PATH 1 byte pipeline (host PTY ↔ TCP/`TCP_NODELAY` ↔ client, with replay-buffer reconnect) is the de-risked core and is fully buildable + unit/integration-testable with **no GUI, no libghostty**. The renderer (libghostty surface in a Metal view) is intrinsically part of the GUI app target and sits behind `TerminalSurface`; its XCFramework build is the only fragile step (needs Zig) and must never block the core.

## Phase → workflow map

| WF | Layer | Deliverable | Verify | Status |
|----|-------|-------------|--------|--------|
| **WF-1** | Foundation | git init, Package.swift (all targets), `AislopdeskProtocol` fully impl + tests, `docs/20-wire-protocol.md` | `swift build` + `swift test` green | ✅ |
| **WF-2** | Transport | `AislopdeskTransport`: `TCP_NODELAY` conn, dual data/control, BackedWriter/Reader replay, reconnect handshake | loopback unit tests (framing, replay-after-drop, reconnect resume) | ✅ |
| **WF-3** | Host PTY | `AislopdeskHost`: openpty+posix_spawn, relay, resize, session mgr; `aislopdesk-hostd` | spawn `cat`/`echo` in PTY, byte round-trip test | ✅ |
| **WF-4** | E2E byte pipeline | `AislopdeskClient` + `aislopdesk-client`; host↔client loopback over TCP | e2e: client sends `echo hi`, receives `hi`; kill+reconnect resumes | ✅ |
| **WF-4b** | Reconnect hardening | fix resume-rebind race (dead-channel send must retain-not-throw; gate on new-channel readiness; client clean-FIN→reconnect) | flaky reconnect test 100× green + suite 3× | ✅ |
| **WF-5** | libghostty seam | `TerminalSurface`, `GhosttySurface` (GUI), Zig install + ghostty fork + External.zig patch + XCFramework script | `swift build` still green w/o framework; build script runs (best-effort) | ✅ infra / ⛔ compile blocked (Zig 0.15.2 ✗ macOS 26.5 SDK; 0.16.0 ✗ fork build.zig) — see log |
| **WF-6** | Inspector | `AislopdeskInspector`: tail JSONL transcript + hooks, NWConnection #2, tool-card/subagent/todo model + SwiftUI views | unit tests on JSONL parse + event model | ✅ |
| **WF-7** | Claude Code integ | host env (TERM=xterm-ghostty, NO_FLICKER, setup-token reuse), input box A+B1 model | unit tests on env + dedup ring | ✅ |
| **WF-8** | Client UI + iOS | `AislopdeskClientUI` (SwiftUI views + view-models binding Client/Inspector/ClaudeCode) + iOS UIKit table-stakes; GhosttySurface render seam (gated); XcodeGen .app projects best-effort | macOS swift test + iOS-triple typecheck; xcodebuild best-effort | ✅ macOS 228-green / iOS typechecks + both .app BUILD SUCCEEDED |
| **WF-9** | GUI video path | `AislopdeskVideoProtocol` (packetizer/FEC/cursor/coord-map, tested) + `AislopdeskVideoHost` (SCKit capture + 2-session VT encode + CGEvent) + `AislopdeskVideoClient` (VTDecode + Metal/CADisplayLink + client cursor), compiled+reviewed (NO live VT/SCK in tests — hang risk) | pure-logic tests + compile; core stays green | ✅ macOS 275-green / iOS typechecks / warning-clean (44 new pure tests; NO VT/SCK/Metal in tests) |
| **WF-10** | Consolidation / handoff | `README.md` front door (run guide) + `docs/21-HANDOFF.md` (per-layer status + test counts + commit hashes + verify-on-hardware) + doc-link sanity; no source/test change | doc links resolve; full `swift test` 3× green | ✅ 278-green 3/3 / 0 broken links / docs match finished state |
| **WF-11** | OS-floor cleanup | raise floor to macOS 26 / iOS 26; strip dead `@available` + old-OS compat branches; Darwin-only PTY | `swift build`/`swift test` still green | ✅ `42c75cd` — ~40 versioned `@available` removed, no compile change |
| **WF-12** | PATH 1 complete + harden | `aislopdesk-hostd --claude`/`--xterm256`; handshake timeout + half-open reaper; idle-TTL session reaper; `TERM=xterm-ghostty`; reconnect double-disconnect fix | new reaper/suppression tests; suite green | ✅ `1a7336a` — PATH 1 end-to-end + hardened |
| **WF-13** | PATH 2 integrate | host+client session orchestrators (`AislopdeskVideoHostSession`/`AislopdeskVideoClientSession`), `NWVideo*` UDP transports, display-link pacing, never-run-bug fixes; `AislopdeskVideoClient` linked into both apps | pure-logic tests only (hang rule); compile incl. iOS | ✅ `cb6b6b9` — COMPILED + reviewed, live pipeline NOT started in app (nil-connection seam) |
| **WF-14** | iOS responder host | `TerminalInputHost` `UIResponder`/`UIViewRepresentable` assembling KeyRepeater + accessory bar + IME proxy + floating cursor → `AislopdeskClient.sendInput`; `scripts/check-ios.sh` iOS-triple harness | iOS-triple build via `check-ios.sh` | ✅ `fa79874` — COMPILES for iOS + reviewed, on-device interaction unverified |
| **WF-15** | title/bell producer | host OSC 0/2 title + BEL sniffer (`HostTitleBellSniffer`) on the output relay → wire types 21/22 now produced end-to-end | sniffer + client E2E tests | ✅ `4d14431` |
| **WF-16** | libghostty renderer wire | `GhosttyTerminalView` (Metal-backed NS/UIViewRepresentable over `GhosttySurface`) + guarded `#if canImport(CGhostty)` `TerminalRendererFactory` registration in `AppMain` | gated/inert on this host (xcframework absent) | ✅ `46416ce` infra · ⛔ compiled by NO build here — written + reviewed only |

Legend: ⏳ running · ✅ done · ⛔ blocked · 🔁 needs-fix

## Sequencing tonight
PATH 1 core is the priority: **WF-1 → WF-2 → WF-3 → WF-4** (sequential; each depends on prior). WF-5 (libghostty) and WF-6 (inspector) are independent of the byte pipeline and can run alongside. WF-7/8/9 follow. Each green layer → atomic git commit. No push (no remote). GUI apps need libghostty + signing → scaffold only.

## Toolchain notes
- Swift 6.3.2 · Xcode 26.5 · arm64 · macOS 26.5. git + gh present.
- `brew install zig pandoc` done → **zig 0.16.0** at `/opt/homebrew/bin/zig`, pandoc 3.9.0.2.
- ⚠️ **WF-5 must pin Zig precisely:** zig 0.16.0 is too new for the ghostty fork (Zig breaks between minors; `daiimus/ghostty:ios-external-backend` was cut from an older SHA needing ~0.13/0.14). WF-5: read the fork's `build.zig.zon` / `.zigversion` / `minimum_zig_version`, fetch that EXACT toolchain into a local dir (ziglang.org/download or mach nominated), and use it via a build-local PATH — do NOT rely on brew's 0.16.0.

## Build log
- (append one line per workflow completion: WF-id · result · commit · notes)
- **WF-1** · ✅ green · SPM foundation (5 libs + 2 execs + 4 test targets, swift-tools 6.0, macOS 14 / iOS 17), `AislopdeskProtocol` fully implemented (framing + dual-channel `WireMessage` + streaming `FrameDecoder`, big-endian, Int64 seq), `docs/20-wire-protocol.md`. `swift build` + `swift test` green (32 tests, 0 failures), warning-free. Skeleton seams for Transport/Host/Client/Terminal + `HeadlessTerminalSurface` real impl. Renderer = libghostty-only behind `TerminalSurface`, no fallback.
- **WF-1 review** · ✅ green · addressed concurrency + decode-coverage review: `ClientConnection` mutable state now `NSLock`-guarded (real `@unchecked Sendable`, no data race); `AislopdeskConnection`/`ReconnectManager` dropped gratuitous `@unchecked` for automatic `Sendable` (so WF-2/WF-4 mutable state will trip the compiler, not be pre-suppressed); added decode error-path tests (`.truncated` on short exit/resize bodies, `.malformedBody` on invalid-UTF-8 `title`, `maxFramePayloadLength` boundary accepted, 256 KiB large-payload round-trip). No wire-format change → `docs/20` unchanged.
- **WF-2** · ✅ green · `AislopdeskTransport` real impl: `TransportParameters.makeTCP()` (single canonical `NWParameters` — `TCP_NODELAY`+keepalive, no app crypto, no interface pin); `NWMessageChannel` actor (NWConnection ⇄ `FrameDecoder`, drains all frames per chunk, async over continuations); pure value-type `ReplayBuffer` (Int64 seq, never-drop invariant, 4 MiB offline gate / 64 MiB cap, `shouldPauseDrain`); `HostTransport`/`HostSessionTransport`/`ClientTransport` actors with server-decides-RETURNING_CLIENT handshake + 1-byte+sessionID channel-association preamble (`docs/20` §8); atomic `resume()` replays the tail in strictly ascending seq before live output resumes. `swift build`+`swift test` green (63 tests, 0 failures, warning-clean; reconnect-resume + framing-fragmentation over real loopback NWConnections, stable across repeated runs). No `@unchecked Sendable` over unguarded mutable state. Wire format unchanged; appended `docs/20` §8 (channel association & handshake).
- **WF-3** · ✅ green · `AislopdeskHost` real impl: `PTYProcess` = `openpty()` (sane cooked termios + `IUTF8` + initial winsize) → `posix_spawn` with `POSIX_SPAWN_SETSID` (createSession), file-actions dup2 slave→0/1/2 + addclose master in child; **controlling tty acquired implicitly** — a fresh session leader's first non-`O_NOCTTY` tty open (the dup2'd slave) becomes its controlling terminal on macOS 26, so **no `TIOCSCTTY` needed** (verified empirically: `tty`→`/dev/ttys*`, `stty size`→openpty winsize, `TIOCSWINSZ`+`SIGWINCH` reflow); `setBlocking(true)` clears `O_NONBLOCK` on master pre-spawn (Happy #301); dedicated `waitpid` reaper thread surfaces exit code as `WireMessage.exit`. `PTYReadLoop` = no-buffer blocking read loop at `QOS_CLASS_USER_INTERACTIVE` → straight to `HostSessionTransport.sendOutput` (only buffer is the transport `ReplayBuffer`); backpressure gate parks the loop (zero master syscalls via `NSCondition`) on `drainPauses==true` so the kernel PTY buffer backpressures the shell, resumes on false. `HostSession` binds PTY↔transport (input→master, resize→`TIOCSWINSZ`, ack, exit); **session survives client disconnect** — `HostTransport` rebinds fresh channels onto the same `HostSessionTransport`+`ReplayBuffer` and replays in place (not re-yielded on `sessions_`), the daemon never kills the shell on channel failure. `HostServer` owns `HostTransport`, spawns a curated-env login shell per NEW session (keep-alive policy default; `TERM=xterm-256color` placeholder // TODO(WF-7)). `aislopdesk-hostd` parses `--port`/`--shell`, binds 0.0.0.0/OS-chosen, logs to stderr, runs to SIGINT (smoke-verified: binds + clean shutdown). `swift build`+`swift test` green (74 tests total, +13 host: PTY round-trip/echo/controlling-tty/resize/exit-code/O_NONBLOCK + backpressure gate + transport drain transitions; 0 failures, warning-clean, stable over 5 full runs). No `@unchecked Sendable` over unguarded mutable state (every one guards an `NSLock`/`NSCondition`). WF-2 public APIs unchanged.
- **WF-3 review** · ✅ green · addressed host PTY/relay/session review: **(fd hygiene)** master fd was leaked on every spawn — added `PTYProcess.closeMaster()` (idempotent) + `deinit` safety net, called from `HostSession.shutdown()` *after* stopping the read loop (so no `read()` races the close); regression test spawns+shuts 40 sessions and asserts ~0 open-fd delta (was +40). **(output ordering — the load-bearing fix)** the relay bridged each PTY chunk onto the transport actor via a fresh detached `Task` per chunk, which does NOT preserve order (independent tasks hop onto the actor in scheduler order, not creation order) → scrambled seq assignment corrupting both the live stream and the replayed tail; replaced with a single ordered FIFO `AsyncStream<Data>` whose one consumer awaits `sendOutput` sequentially (actor-arrival order == read order). New `RelayOrderingTests` drives the real relay with a 20k-line burst and asserts a perfect contiguous run — **empirically confirmed it FAILS on the old per-chunk-Task bridge and PASSES on the FIFO** (test is regression-meaningful, not vacuous). `resume()`'s ascending-seq replay is now correct as written (no change there). **(missed-exit on reconnect)** `sendExit` now records the exit code and is gated behind `isResuming`; `resume()` re-sends it after the tail flush so a client offline when the shell exits still gets the terminal marker (no "zombie session"); offline `sendExit` records-not-throws. **(spawn-failure leak)** added `HostTransport.dropSession(_:)` + `HostSessionTransport.close()` (cancels forwarders, closes channels, finishes inbound streams); `HostServer` calls it on `pty.spawn()` failure so the already-bound+published session is torn down instead of orphaned with live channels. **(test coverage)** strengthened `testControllingTTY` to exercise `/dev/tty` (`tty </dev/tty; stty size </dev/tty`) — empirically proven regression-meaningful for `POSIX_SPAWN_SETSID` (WITH setsid → `/dev/tty`+`40 132`; WITHOUT → `Device not configured`), where the old fd-0 form passed even with setsid broken; added a WIFSIGNALED test (self-`SIGTERM` → 143). `swift build`+`swift test` green (81 tests, +7; 0 failures, warning-clean, stable over 3 full runs). WF-1/WF-2 public APIs unchanged; only additive new public methods (`closeMaster`, `HostSessionTransport.close`, `HostTransport.dropSession`). No wire-format change → `docs/20` unchanged.
- **WF-4** · ✅ green · **PATH 1 is a WORKING END-TO-END SYSTEM.** `AislopdeskClient` (actor on WF-2 `ClientTransport`): `connect(host:port:)` (NEW session = zero sessionID), merged inbound → gap-free/dup-free `output: AsyncStream<Data>` + `events` (title/bell/exit/disconnected/reconnected) + optional `TerminalSurface` feed; **ack policy** = track highest-contiguous `output.seq`, coalesced background ticker flushes `sendAck` ≤ every 50ms (never acks an undelivered seq — correctness over cadence); **reconnect dedup** = drop any inbound `output` with `seq <= highestSeqFed` so a replayed tail splices with no gap/dup; **iOS lifecycle seam** `pause()`/`resume()` (proactive teardown on background, byte-exact resume on foreground — WF-8 wires UIKit). `ReconnectManager` rewritten to drive `AislopdeskClient` with exponential backoff capped at **2s** (DECISIONS §reconnect: fast re-grab, not minutes) presenting preserved `sessionID`+`lastReceivedSeq`. New `AislopdeskTTY` library (split out so it is unit-testable) = termios `cfmakeraw` raw mode with restore on EVERY exit path (defer + `sigaction` SIGINT/TERM/QUIT/HUP re-raise + explicit on disconnect/exit, async-signal-safe `tcsetattr`) + `TIOCGWINSZ`/`TIOCSWINSZ`. `aislopdesk-client` executable = genuinely usable interactive remote terminal (`--host/--port/--no-raw`): raw-mode relay stdin→input (single ordered AsyncStream consumer — keystroke order preserved, WF-3 lesson) + output→stdout, initial + SIGWINCH resize, **Ctrl-] disconnect** (restores terminal, exit 0); non-interactive pipe mode waits for session exit so a piped `echo X\nexit\n` never truncates. **Crown-jewel e2e use REAL components:** in-process echo over loopback (HostServer spawns `/bin/sh` → `echo aislopdesk-e2e-OK` round-trips + exit surfaces); **byte-exact reconnect resume** (host emits ordered `line-1..2000`, client acks partway, FORCE-DROP NWConnections w/o `bye`, reconnect via `ReconnectManager`, assert reconstructed stream == exactly `1..2000` once each in order — proves session survival + replay + dedup together; **fails 7/8 with a premature `sawDone` wait, passes 8/8 with the corrected `maxLine>=N` completion gate** — the test caught its own flake, the system was lossless); subprocess e2e launches the actual shipped `aislopdesk-hostd`+`aislopdesk-client` binaries (ephemeral port from hostd stderr, `--no-raw` piped stdin) and asserts `SHIPPED_OK` in stdout; ack-correctness (never acks unreceived seq; `acknowledge` drops retained bytes); raw-mode save/restore byte-exact + SIGWINCH→resize over an `openpty` pair. `swift build`+`swift test` green (**93 tests, +12; 0 failures, warning-clean, stable across 3 full runs** + reconnect 8/8 + subprocess 4/4). Swift-6 actor isolation throughout, no `@unchecked Sendable` over unguarded state (the few `@unchecked` boxes guard an `NSLock`/`os_unfair_lock`). WF-1/2/3 public APIs unchanged (the `HandshakeReconnectTests` transient was not reproduced; the reconnect teardown race is correctly absorbed by `try?` on post-cancel sends in both relay and client). No wire-format change → `docs/20` unchanged. **Run it:** `aislopdesk-hostd --port 7420` on the host, `aislopdesk-client --host <H> --port 7420` on the client (over NetBird in prod).
- **WF-6** · ✅ green · **`AislopdeskInspector` — the read-only structured companion (differentiator), fully INDEPENDENT of the terminal byte pipeline** (reuses only AislopdeskProtocol's length-prefix framing *style*, never the terminal `WireMessage`; read-only by construction — observes the transcript, never drives the agent). **(1) Transcript model + tolerant JSONL parsing** (`TranscriptLine`/`TranscriptParser`/`JSONValue`): discriminated-union on `type` (user/assistant/meta), schema-free tool `input` kept as a `JSONValue` tree; unknown `type` → `.unknown(raw:)` and internal types (`file-history-snapshot`/`queue-operation`/`rate_limit_event`) → `.ignored` — **unknown/malformed lines never crash** (doc 16 "schema chỉ stable mức union → passthrough"). **(2) Typed `InspectorEvent` taxonomy** the client renders from: `toolCard`/`todosUpdated`/`subagentUpdated`/`subagentToolCard`/`thinking`/`message`/`sessionStarted`/`workflow`/`unknownLine`. **Tool cards** pair `tool_use`→`tool_result` by id (`pending`→`completed`/`errored`); handles out-of-order (result-before-use held then resolved once), missing result (stays `pending`), error (`is_error`); dedup via `processedKeys` (`uuid` / `sidechain:<agentID>:<uuid>`) so a re-read tail never double-emits. **Todos** parsed from `TodoWrite`/`Task*` payloads (latest-state). **CoT/thinking = PLACEHOLDER ONLY** (doc 16 load-bearing): `thinking:""`+`signature` → `isPlaceholder` marker, **never fabricated**. **Subagent tree** (doc 16 refuted-correction): subagent content lives in separate `subagents/agent-<hash>.jsonl` files linked via `SubagentStop.agent_transcript_path`; nodes index by `agentId`, sort in-level. **Workflow = defer/preview** (minimal `WorkflowMarker`). **(3) Host tailer** `TranscriptTailer` = **poll size + read-delta** append-follow (documented choice over DispatchSource vnode: portable macOS+iOS, deterministic, unit-testable; JSONL flushes per-turn so poll latency is fine, the low-latency path is the PostToolUse hook) through a pure `LineAccumulator` that **holds a partial last line until its newline** and **resets on truncation/rotation** (file shrank); discovers the path from a `SessionStart` hook (`transcript_path`, never reconstructed from cwd) and tolerates the file-not-yet-created race. `SubagentWatcher` polls the `subagents/` dir for new `agent-*.jsonl` and tails each once. `HookIngest` seam = typed `HookPayload` (SessionStart/PostToolUse/SubagentStop) + `HookParser` + fold-into-stream; PostToolUse arriving before the JSONL flush dedups on the same card id. `InspectorEngine` actor combines all sources through one `EventBuilder` → ordered `AsyncStream<InspectorEvent>`. **(4) Second-channel transport (NWConnection #2):** separate `InspectorWireMessage` namespace (`event`/`keepAlive`/`subscribe`) + length-prefixed **JSON** frames (event rate low, schema rich — JSON wins; doc 16 "length-prefixed JSON frames"), `InspectorCodec`/`InspectorFrameDecoder` mirror AislopdeskProtocol framing (4-byte BE length + 1-byte tag, 16 MiB cap) without coupling; `ByteChannel` abstraction with a production `NWByteChannel` (TCP_NODELAY, no app crypto) and an in-process `LoopbackByteChannel` for deterministic tests; host `InspectorSource` (events host→client) + client `InspectorClient` (→ `InspectorEvent` stream); read-only — the only client→host message is a lightweight `subscribe(fromSeq:)` replay control, no agent input. **(5) SwiftUI views** (compile, logic-free): `InspectorPane` + `ToolCardListView`/`TodoListView`/`SubagentTreeView` (collapsible `DisclosureGroup`)/`ThinkingIndicatorView`, driven by an `@MainActor @Observable InspectorViewModel` that owns ALL projection (card upsert, subagent tree build, todo/thinking/session state); views only lay out. `swift build`+`swift test` green (**128 tests total, +32 inspector; 0 failures, warning-clean, stable across repeated runs** incl. the timing-sensitive append-follow tailer tests 3/3). Fixtures under `Tests/AislopdeskInspectorTests/Fixtures/` (realistic main-session JSONL with user/assistant/tool_use/tool_result/TodoWrite/thinking/unknown/internal lines; a `subagents/agent-deadbeef.jsonl`; SessionStart/PostToolUse/SubagentStop hook payloads). Package.swift: new `AislopdeskInspector` lib (dep AislopdeskProtocol) + `AislopdeskInspectorTests`. WF-1..4b public APIs unchanged; no terminal wire-format change.
- **WF-7** · ✅ green · **Claude Code integration LOGIC** (the testable env + detection + dedup; the compose-box UI is later WF-8 GUI work). **(1) Host launch env (`AislopdeskHost`, the WF-7 seam).** `ClaudeCodeProfile` builds the curated child env by merging FORCED Claude keys over a SANITIZED inherited allowlist — it never copies the parent env wholesale. **Forced** (always overwrite): `TERM` (`xterm-ghostty` default, with a first-class `.xterm256` toggle → `xterm-256color` that mitigates the multi-line paste bug #54700 by dropping DEC 2026), `COLORTERM=truecolor`, `CLAUDE_CODE_NO_FLICKER=1` (fullscreen for remote PTY), `CLAUDE_CODE_ENTRYPOINT=remote_mobile` (non-SDK headless resume). **Inherited** (passed through only when present, never clobbered): `HOME`/`USER`/`LOGNAME`/`SHELL`/`PATH`/`TMPDIR`/`LANG`/every `LC_*`/`TERM_PROGRAM` (forced/inherited sets are disjoint; `LANG` defaults to `en_US.UTF-8` only when absent). **Auth resolution is STAT-ONLY (privacy-critical):** `ClaudeAuthResolver` returns a typed `AuthStrategy` — `.inheritedCredentials(path:)` when `~/.claude/.credentials.json` EXISTS (resolved via `FileManager.fileExists`, a `stat`; the spawned `claude` then inherits it via `HOME`) or `.setupToken(needed:)` otherwise (`claude setup-token` 1-year headless path). **The resolver has NO capability to read the file** — it takes only a `(String)->Bool` existence predicate, so it can never open/log/transmit the credential bytes; tests prove this (fixture temp file, assert on path/existence + exactly-one-stat, never the contents; the real `~/.claude/.credentials.json` is never touched). `HostServer.LaunchMode` (`.shell` default vs `.claudeCode(profile)`) selects `[shell, -lc, claude]` with the profile env vs. the unchanged WF-3 plain-shell path; a `// TODO(aislopdesk-hostd)` marks where the daemon exposes a `--claude` flag (logic only, no CLI over-build). **(2) `AislopdeskClaudeCode` (NEW cross-platform target, dep AislopdeskProtocol).** `TerminalModeTracker` = a hand-rolled, byte-at-a-time escape-sequence sniffer (NOT a full VT parser — libghostty's surface is opaque, doc 14): tracks `.shellPrompt` ⇄ `.altScreen` via DECSET/DECRST `?1049h/l` (+ legacy `?47`/`?1047`) and emits OSC 133 command-boundary events (`promptStart`/`commandStart`/`commandStarted`/`commandFinished(exitCode?)` from `;A`/`;B`/`;C`/`;D[;exit]`). **Split-sequence robustness is the load-bearing property** (the #1 thing that silently breaks): the machine holds its partial CSI/OSC state between `consume(_:)` calls, so a sequence split mid-ESC / mid-CSI / mid-OSC / one-byte-at-a-time yields BYTE-FOR-BYTE identical events to feeding it whole — asserted by feeding a rich interleaved stream at EVERY chunk size `1...n`. Tolerant: unknown CSI (`?2004h`, `?25l`, SGR) and unknown OSC (0/8/52) are consumed to their terminator and ignored; high-bit / invalid-UTF-8 bytes pass through; an overlong unterminated OSC is capped so a hostile stream can't wedge the parser. `TerminalModeStream` is the `AsyncStream<TerminalModeEvent>` façade. **(3) `InputDedupRing` (input-box B1).** Suppresses the PTY's echo of compose-box input (doc 14: Happy/Happier duplicate-prompt lesson) via a **hold-and-confirm** model — a byte that merely shares a prefix with the expected echo is HELD, not optimistically dropped, and is FLUSHED back intact the moment the match diverges (so e.g. the `l` in `total` is never eaten); a full match drops the held run. Handles exact echo, partial echo split across chunks (held run + cursor persist between `filter` calls), non-echo passthrough, PTY newline transform (`\n`→`\r\n`), and a bounded FIFO ring (`capacity` bytes, eviction retreats the cursor). `InputBoxModel` ties tracker + ring together → `InputAffordance` (`.shellCommand` 'A' at the prompt / `.tuiCompose` 'B1' in alt-screen), drives the ring ONLY in B1 (shell-A shows echo, ring reset), tracks command-running/exit-code, and resets dedup state on every mode flip. **(4) Tests** (XCTest, byte-sequence + fixture based; +53): env forced/inherited/toggle/no-clobber, auth fixture-present/absent/stat-only, sniffer transitions + the all-chunk-sizes split-boundary equivalence + tolerance, dedup exact/partial/non-echo/eviction/prefix-flush, input-box A↔B1 transitions + B1-only dedup. `swift build`+`swift test` green (**181 tests total, +53; 0 failures, warning-clean, stable across repeated runs**). Package.swift: new `AislopdeskClaudeCode` lib (dep AislopdeskProtocol, builds macOS+iOS) + `AislopdeskClaudeCodeTests` (dep AislopdeskClaudeCode+AislopdeskHost). WF-1..6 public wire APIs unchanged; no terminal wire-format change → `docs/20` unchanged. The interactive compose-box UI itself is deferred to WF-8 (GUI).
- **WF-4b** · ✅ green · **fixed the reconnect resume-rebind race** (the `HandshakeReconnectTests` flake, reproduced ~1/40 on `testReconnectWithReceivedAboveAckedReplaysNothing`). **Root cause (object-identity traced):** a live `HostSessionTransport.sendOutput(seq=N+1)` captured the current `.ready` data channel and suspended inside `NWConnection.send`; a concurrent RETURNING_CLIENT `resume()`→`swapChannels` reassigned `self.dataChannel` to the fresh channel and closed the **old** one in its detached `Task`, cancelling the very channel the in-flight send was writing to → `NWConnection.send` completed with POSIX 89 (`ECANCELED`). The catch gated on `isChannelDown` (a re-read of the old channel's `currentState`), but the send-completion error and the `.cancelled` `stateUpdateHandler` are **two independent NWConnection callbacks**: the error raced ahead of the state notification, so `currentState` still read `.ready` → the error was re-thrown fatally. **Fix (retain-not-throw, no symptom patch):** (1) `NWMessageChannel.send` now classifies a completion error whose `NWError` is `.posix(.ECANCELED)` as the typed `AislopdeskTransportError.notConnected` ("channel gone" contract) instead of `.sendFailed`; (2) `HostSessionTransport.sendOutput`/`sendExit` detect "channel lost" robustly **without** relying on the racy state re-read — the bound channel was swapped out from under the send (`self.dataChannel !== captured`), the error is `.notConnected`, or the re-read state is `.cancelled`/`.failed` — and in all cases **retain-and-return** (bytes already in the `ReplayBuffer`, `highestSentSeq` advances only on a *successful* send and only while the channel is still current, so the resume's replay loop re-sends them on the new channel: no loss, no fatal throw); (3) post-await `self.dataChannel === captured` guard so a stale success on a swapped-out channel cannot block the resume flush. **Toolchain trap caught:** factoring the channel-lost decision into a shared actor-isolated `async` helper awaited from the catch block **miscompiled** (`EXC_BAD_INSTRUCTION` on the cooperative pool, surfacing deterministically under the `RelayOrderingTests` 20k-line burst — bisected with stash + lldb, baseline 5/5 green vs. helper-form 100% crash); the detection is therefore written **inline** at both catch sites (generates correct code). Client clean-FIN→reconnect symmetry (`ClientTransport.startDataForwarding` `finishInbound(error: nil)` on FIN → `.disconnected` → `ReconnectManager`) verified intact (no silent stall). **New deterministic tests:** `MessageChannelLoopbackTests.testSendOnCancelledChannelSurfacesNotConnectedNotSendFailed` (50× — cancelled-channel send must be `notConnected`, never `sendFailed`); `HostSessionTransportTests.testRebindWindowSendRetainsAndReplaysByteExact` (40× — fires a live `sendOutput` concurrently with a `resume()` that swaps+closes the old channel, asserts no throw + the new channel carries seq `1..N+1` gap-free/dup-free/byte-exact). **Verification:** `HandshakeReconnectTests` **100/100** consecutive; full `swift test` **3/3 green** (96 tests, 0 failures, warning-clean). Never-drop invariant (buffer evicts only on ack) + client-side seq dedup + strictly-`(lastReceivedSeq, N]` replay range all unchanged and re-verified by the existing byte-exact/dedup tests. Wire/handshake format unchanged; documented the dead-channel-send=retain invariant + client clean-FIN=reconnect rule in `docs/20` §8.3, `HostSessionTransport`, and `ReplayBuffer` doc comments.
- **WF-5** · ✅ infra green / ⛔ compile BLOCKED-by-toolchain×SDK (honest) · **libghostty external-IO build infra + the `GhosttySurface` Swift binding — renderer = libghostty-ONLY (no SwiftTerm, no fallback).** **Renderer fork pinned (approach b):** `daiimus/ghostty @ ios-external-backend`, **SHA `21c717340b62349d67124446c2447bf38796540b`**, **Zig `0.15.2`** (the fork's `build.zig.zon` `minimum_zig_version`; SHA-256-pinned tarball). Chose pinning the daiimus SHA *directly* over authoring our own upstream patch because the external-IO C API exists ONLY on the forks (verified: NOT in upstream `include/ghostty.h`) and daiimus already ships a complete `src/termio/External.zig` (~470 LOC + resize callback + tests) plus the C glue — zero rebase risk for zero benefit vs. an own-patch. **API correctness (the load-bearing deliverable):** inspected the pinned `include/ghostty.h` (1369 lines, vendored verbatim at `integration/CGhostty/ghostty.h`) and confirmed the ACTUAL symbols differ from the loose spec names — Data IN = `ghostty_surface_write_output(s,ptr,len)` (NOT `feed_data`); Data OUT = the `write_callback` field set at `ghostty_surface_new` (NOT `set_write_callback`); backend select = `config.backend_type = GHOSTTY_BACKEND_EXTERNAL` (the `use_custom_io` concept); resize = `ghostty_surface_set_size` (pixels) + `resize_callback`; keys = `ghostty_surface_key`; text = `ghostty_surface_text`; `self` recovered in C callbacks via `config.userdata`→`ghostty_surface_userdata`. `GhosttySurface` is `@MainActor` to enforce the doc-18-§C main-thread contract (`write_output`/`refresh`/`draw` are main-only; feed→refresh→draw kept synchronous to avoid actor-suspension escape), routes every key via `ghostty_surface_key` (DECISIONS), and conforms to the existing `AislopdeskTerminal.TerminalSurface`. **Core stays 187-green:** the binding + `CGhostty` module map are under `ThirdParty/ghostty/integration/`, wired into NO `Package.swift` target — a plain `swift build`/`swift test` never sees them (the WF-8 GUI app target will), so zero conditional-compilation hacks in the core. **Deliverables committed:** `ThirdParty/ghostty/{build-libghostty.sh (idempotent: SHA-verified pinned-Zig download + pinned-fork clone + zig build -Demit-xcframework + nm symbol verify; time-boxed + libSystem preflight), External.zig.patch (documented source delta), README.md (full integration story + data-flow + threading + GUI-link + honest blocker), integration/CGhostty/{module.modulemap,ghostty.h}, integration/GhosttySurface/GhosttySurface.swift}`. Gitignored (derived/large): `.toolchain/`, `.work/`, `*.xcframework/`. **BUILD OUTCOME — honest:** the script ran end-to-end up to the LINK step (Zig 0.15.2 downloaded+SHA-verified, fork pinned, external-IO symbols confirmed in source, ~15 zig deps fetched) then FAILED — **blocked by a Zig↔SDK incompatibility, not a script/fork/binding bug.** Empirically characterized both jaws of the pincer on this M-series macOS **26.5** host: (1) the pinned **Zig 0.15.2 cannot link the macOS 26.5 SDK** — even a trivial `zig run` errors `undefined symbol: __availability_version_check`/`_abort`/`_bzero` (0.15.2 predates the 26.x libSystem layout; `--sysroot`/`-lc` don't help; symbol IS in `libSystem.tbd`); (2) the only SDK-compatible Zig here, **0.16.0, is rejected by the fork's `build.zig`** on a hard `requireZig` gate AND `std.process.EnvMap` removal. So no satisfying toolchain on a macOS-26.5-only host (doc 19 anticipated "brew 0.16.0 too new"; the inverse "0.15.2 too old for the 26.5 SDK" closes it). The xcframework will build on a host with an older SDK (≤15.x / an Xcode-16 CLT or CI runner), or after a future Zig that supports both, or after bumping the fork pin to a 26-capable `build.zig` (re-verify symbols). `swift build`+`swift test` **187/187 green** with all WF-5 files present (core untouched). WF-1..7 public/wire APIs unchanged; no terminal wire-format change → `docs/20` unchanged.
- **WF-8** · ✅ macOS green / iOS typechecks / xcodebuild ✅ (honest) · **`AislopdeskClientUI` — the cross-platform SwiftUI client + iOS UIKit native-feel table-stakes, built ON TOP of WF-1..7 with NO change to any existing module's public API.** **(1) Cross-platform SwiftUI (compiles under macOS `swift build`).** `TerminalScreenView` hosts a `TerminalRenderingView` SEAM (protocol): production = a `GhosttyTerminalView` (Metal-hosted `GhosttySurface`) the app target injects via `TerminalRendererFactory.shared`; the no-framework case shows a clearly-labelled BUILD-STATUS placeholder (`BuildStatusPlaceholderView` → "libghostty renderer not built — run ThirdParty/ghostty/build-libghostty.sh"), **NOT a substitute VT renderer** (libghostty-only policy, DECISIONS — the placeholder is build-status telemetry, it does not emulate a terminal). `TerminalViewModel` (`@MainActor @Observable`) consumes `AislopdeskClient.output` (feeds the `TerminalSurface` seam + byte-count telemetry) + `events` (title/bell/exit/disconnected/reconnected) via two concurrent `async let` pumps and projects a `ConnectionStatus`. `InspectorPanel` composes the EXISTING `AislopdeskInspector.InspectorPane` (tool-card timeline / subagent tree / todos / thinking-placeholder) driven by `AislopdeskInspector.InspectorViewModel` over an `InspectorClient` event stream (NWConnection #2, read-only `subscribe`→`consume`). `ConnectionView`+`ConnectionViewModel` = host/port entry, connect/disconnect, live status (connecting/connected/reconnecting/failed) + session id, owning a `AislopdeskClient` + `ReconnectManager` (deliberate-disconnect vs network-drop distinction so a deliberate close does NOT reconnect). `InputBarView`+`InputBarModel` = the external input affordance driven by `AislopdeskClaudeCode.InputBoxModel` (A shell-command vs B1 TUI-compose), sending via `AislopdeskClient.sendInput` and recording B1 sends into the dedup ring (logic stays in `AislopdeskClaudeCode`; this is the view + a thin observable shell). `ClientRootView` = split layout (terminal + toggleable inspector — `#if os(macOS)` side-by-side, `#if os(iOS)` bottom-sheet) with the input bar pinned; `AislopdeskClientApp` (`App` scene) wires it + drives the iOS scene-phase → `AislopdeskClient.pause()`/`resume()` byte-exact-resume seam. **(2) iOS UIKit native-feel table-stakes (doc 17 §2.5) — logic PURE + macOS-unit-tested, UIKit wrappers `#if os(iOS)` + iOS-triple-typechecked.** `KeyRepeater` = manual repeat (UIKit fires presses ONCE): immediate fire → 350ms initial delay → 50ms (20Hz) ramp, **injectable `RepeatScheduler`** (prod `DispatchRepeatScheduler` = `DispatchSourceTimer`; tests `ManualRepeatScheduler` = virtual time) so the exact cadence + stop-on-release + last-key-wins are asserted WITHOUT real time. `FloatingCursorMapping` = pure quantised delta→arrow accumulator (5pt threshold, SwiftTerm-verified; carries sub-threshold remainder; `4pt→[]`, `6pt→[.right]`, `-12pt→[.left,.left]`) + `ESC[C`/`ESC[D` byte encoding, wrapped by the iOS `FloatingCursorController` driving `updateFloatingCursor`. `KeyboardAccessoryDecision` = pure ~150pt keyboard-frame-height threshold (sw-keyboard show / hw-keyboard hide), wrapped by the iOS `KeyboardAccessoryBar` (Ctrl/Esc/Tab/arrows row, sticky-Ctrl control-code fold). `InputRouting` = pure IME-vs-key routing (special keys + Ctrl/Alt/Cmd combos → key encoding; plain/CJK text → the hidden `IMEProxyTextView` `UITextView` so multi-stage composition commits correctly). **(3) Xcode .app scaffolds (XcodeGen).** `Apps/{ClientApp-macOS,ClientApp-iOS}/project.yml` (source-of-truth; generated `*.xcodeproj` gitignored) each depend on the local SwiftPM product `AislopdeskClientUI`; `@main` = `Apps/Shared/AppMain.swift` → `AislopdeskClientApp`; Info.plist + entitlements declared inline in project.yml (regenerable) — macOS client is **sandboxed** (`app-sandbox` + `network.client` only; no Accessibility/CGEvent — those are the PATH 2 HOST's, doc 06), iOS is standard. The `GhosttyTerminalView` wiring is documented in `AppMain.swift` (`TerminalRendererFactory.shared = …`) to add once the xcframework exists. **(4) Tests (AislopdeskClientUITests, +41 → 228 total).** KeyRepeater cadence via the injected virtual-time scheduler (immediate→350ms→50ms, stop-on-release, unrelated-keyUp-ignored, last-key-wins, idempotent re-down); floating-cursor delta→arrow mapping (the spec's 4/6/-12pt cases + accumulation + exact-threshold + byte encoding); accessory-bar show/hide decision (0/55/149 hide, 150/336 show); IME-vs-key routing (plain/CJK→proxy, Ctrl/Alt/Cmd/special→key); `TerminalViewModel` state transitions (first-byte→connected, title/bell/exit/disconnected/reconnected, surface-feed mirror, reset); `InputBarModel` A↔B1 affordance + B1-only dedup + submit CR encoding + command-running; `ConnectionViewModel` driven against a **REAL in-process HostServer + AislopdeskClient over loopback** (connect→connected→live-output, invalid/dead-port→failed, deliberate-disconnect→no-reconnect); `InspectorPanel` data path (InspectorSource→loopback→InspectorClient→view-model fold + card upsert). **VERIFICATION (honest):** macOS `swift build`+`swift test` **228/228 green** (+41; 0 failures, warning-clean, stable across repeated runs incl. the loopback e2e 41/41 twice). **iOS typecheck PASSES** via the SPM iOS-triple build — `SDK=$(xcrun --sdk iphonesimulator --show-sdk-path); swift build --target AislopdeskClientUI -Xswiftc -sdk -Xswiftc "$SDK" -Xswiftc -target -Xswiftc arm64-apple-ios17.0-simulator -Xcc -isysroot -Xcc "$SDK" -Xcc -target -Xcc arm64-apple-ios17.0-simulator` (the `-Xcc -isysroot/-target` are load-bearing: without them clang loads the macOS sysroot and `UIKit` fails to import; with them all `#if os(iOS)` UIKit code compiles + is warning-clean). **xcodebuild BOTH .app targets BUILD SUCCEEDED** unsigned: macOS `xcodebuild build -scheme ClientApp-macOS -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` → `Aislopdesk.app`; iOS `-scheme ClientApp-iOS -destination 'generic/platform=iOS Simulator' …` → `Aislopdesk.app` (Debug-iphonesimulator) — the iOS app build is a SECOND, stronger confirmation the iOS path links, not just typechecks. **One honest non-blocking warning** surfaced ONLY by xcodebuild's package-graph scanner (NOT by `swift build`/`swift test`): `'AislopdeskClient' is missing a dependency on 'AislopdeskTerminal'` — `AislopdeskClient.swift` has a latent `#if canImport(AislopdeskTerminal) import AislopdeskTerminal` while its target omits the edge; left UNTOUCHED to honour "do not modify existing modules" (the core `swift build` stays warning-clean; the fix, if wanted later, is the additive `AislopdeskTerminal` dep on the `AislopdeskClient` target). All existing public/wire APIs unchanged; `AislopdeskClientUI` only consumes them. No terminal wire-format change → `docs/20` unchanged.
- **WF-8 review** · ✅ macOS 231-green / iOS typechecks · addressed the client-UI / iOS-table-stakes review. **(a) events multicast (the load-bearing fix).** `AislopdeskClient.events` was a SINGLE `AsyncStream` (built once in `init`); `AsyncStream` is single-consumer / fan-in, so the THREE legitimate concurrent consumers (`ReconnectManager` watching `.disconnected`, `ConnectionViewModel.observeEvents`, `TerminalViewModel.pumpEvents`) each stole events nondeterministically — a `.disconnected`/`.reconnected`/`.title` reached only one loop, so the reconnect supervisor could miss the drop and the chrome/terminal statuses diverged. Added `EventBroadcaster<Event>` (NSLock-guarded tee: each `events` access subscribes a fresh child stream, one `yield` fans to ALL children, `finish()` ends them); `events` now returns `eventBroadcaster.subscribe()` — **same `nonisolated var events: AsyncStream<Event>` signature, WF-4 API unchanged.** ALSO collapsed the UI to ONE events consumer: `TerminalViewModel.observe(client:)` now pumps `output` ONLY (no events loop); `ConnectionViewModel.observeEvents` is the single UI events loop and forwards EVERY event to `terminal.handle(_:)` (one loop, two folds). **(b) end-to-end consistency tests** (`ConnectionViewModelTests`): a real force-drop→`ReconnectManager`-resume cycle asserts chrome `status` AND terminal `connectionStatus` both converge `.connected` (not diverged); a `.title` injected through the live stream (`_handleInboundForTesting`) is asserted to reach the terminal model while the chrome loop runs. **(c) KeyRepeater data race.** `heldKey`/`handle` were mutated from the main thread (`keyDown`/`keyUp`) AND reassigned/read from the background `DispatchRepeatScheduler` queue with no lock — an unsynchronised race masked by the synchronous `ManualRepeatScheduler` in tests. Guarded both with an `NSLock` (lock held only around state read/write; `onFire`/`scheduler.schedule` run outside it, so a re-entrant or synchronous-firing scheduler cannot deadlock); a freshly-scheduled handle is adopted only if its key is still held (else cancelled), so a racing `keyUp`/`keyDown` never leaves a stale timer armed. **(d) production-scheduler test** (`KeyRepeaterTests.testDispatchSchedulerFiresAndStopsOnRelease`): drives a `KeyRepeater` with the REAL `DispatchRepeatScheduler` + an `XCTestExpectation` sink (await fulfillment, not sleep) asserting ≥2 fires via the actual `DispatchSourceTimer` then zero fires after `keyUp`; **the full `KeyRepeaterTests` suite passes under `swift test --sanitize=thread` (TSan clean — the race fix is verified, not just asserted).** **(e) dead-import removal.** Deleted the inert `#if canImport(AislopdeskTerminal) / import AislopdeskTerminal / #endif` block in `AislopdeskClient.swift` (unused — the terminal surface is a `@Sendable (Data)->Void` closure seam by design); this eliminates the xcodebuild package-graph-scanner warning `'AislopdeskClient' is missing a dependency on 'AislopdeskTerminal'` WITHOUT adding the dep the closure-seam design deliberately avoids. Core `swift build`/`swift test` were already clean (the import was inert there). **(f) §2.5 status — honest:** the iOS table-stakes are **logic-complete + unit-tested + iOS-triple-typechecked, integration PENDING** — `KeyRepeater`/`KeyboardAccessoryBar`/`IMEProxyTextView`/`FloatingCursorController` have no owning `UIResponder`/`UIView` yet (no `pressesBegan`/`pressesEnded` caller, no `inputAccessoryView` host, no `IMEProxyTextView.onText` consumer, no `updateFloatingCursor(at:)` caller); `InputBarView` wires none of them. The UIResponder host that routes presses→`KeyRepeater`, hosts the accessory bar gated by `KeyboardAccessoryDecision`, embeds the IME proxy, and drives the floating cursor is a follow-up (it is iOS-only view glue, not unit-testable on macOS). **macOS `swift build`+`swift test` 231/231 green** (+3; 0 failures, warning-clean); **iOS-triple typecheck of `AislopdeskClientUI` PASSES, warning-clean.** No public/wire API change (WF-1..7 unchanged): `EventBroadcaster` is `internal`; the only behavioural change is `events` now broadcasts instead of single-fanning.
- **WF-10** · ✅ docs consolidation / final stability green · **Wake-up handoff — top-level docs now match the finished state; no source/test change.** Rewrote `README.md` into the project front door (what-it-is; the 3 data paths ① terminal PTY→TCP→libghostty / ③ read-only inspector / ② GUI video Phase 4; the 14-row module map; **verified quickstart** — `swift build` / `swift test` 278, `.build/release/aislopdesk-hostd --port N`, `aislopdesk-client --host H --port N` with `Ctrl-]` + `--no-raw` pipe form, all real over TCP loopback/NetBird; the honest libghostty blocker + the exact resolve path; a status table). Wrote `docs/21-HANDOFF.md` (per-layer status with **test counts + commit hashes** pulled from this log; COMPLETE+tested-headlessly vs COMPILED+reviewed-not-run with the SCKit/VideoToolbox-hang reason cited to `research/spikes/vtbench/RESULTS.md`; the one external blocker + precise fix; verify-on-hardware checklist; priority-ordered next steps; honest caveats — XOR-parity-not-Reed-Solomon FEC, CoT placeholder-only, stat-only auth, iOS UIKit glue not integrated, GUI video built-to-spike-not-run). **Doc-link sanity:** grepped every relative `.md` link in README + docs/00/19/20/21 + ThirdParty/ghostty/README — **all resolve, zero broken links**. **Stability:** confirmed no orphaned `aislopdesk-hostd`/`aislopdesk-client`/`xctest` processes, then ran the FULL `swift test` **3×** — **278 tests, 0 failures all three runs** (~17.8s each), warning-clean, no orphans after. Verified the headline numbers against reality: `swift test` = 278; `git log` = WF-1..WF-9 committed; Package.swift = 22 build targets (12 lib + 2 exec + 8 test); ~17.6k Swift LOC (Sources+Tests+ghostty integration). No `Package.swift`, `Sources/`, or `Tests/` change.
- **WF-9** · ✅ macOS 275-green / iOS typechecks / warning-clean · **GUI video path (PATH 2 / Phase 4) — three new SwiftPM targets, built to the MEASURED spike configs (`docs/research/spikes/vtbench/`, docs 17/18) — NOT re-derived.** **HANG-SAFETY honoured throughout: NO test instantiates an `SCStream`, `VTCompressionSession`, `VTDecompressionSession`, or Metal device** (capture + HW encode hang without a window-server + Screen-Recording TCC session — RESULTS.md); the host/client video code is COMPILED (`swift build`, incl. the iOS triple) + code-reviewed, never executed in a test (verified: `grep` of `Tests/` finds zero `VideoToolbox`/`ScreenCaptureKit`/`Metal`/`SCStream`/`VTCompressionSession`/`VTDecompressionSession` imports). **(A) `AislopdeskVideoProtocol` (cross-platform, PURE, leaf — zero platform dep, builds macOS + iOS, the only target with tests).** UDP frame transport: `VideoPacketizer` fragments an AVCC frame into `<=1200`-byte datagrams with a 15-byte header (`UInt32` streamSeq + `UInt32` frameID + `UInt16` fragIndex + `UInt16` fragCount + flags{keyframe,parity,crisp} + `UInt16` payloadLen); `FrameReassembler` reassembles by frameID, detects loss, and surfaces unrecoverable losses via a `nextDroppedFrame()` queue (so completing a NEWER frame never hides an older loss → still signals recovery). `NALUnit` = defensive length-prefixed AVCC split/join (macOS 26 emits 1 NALU but we iterate anyway, doc 18; a bad prefix stops cleanly, never crashes). `FECScheme` protocol + `XORParityFEC` (groupSize 5 = 20% parity, doc 17 §3.6) with REAL single-loss-per-group recovery — length-prefixed XOR so differently-sized fragments recover byte-exact (production may swap in Reed-Solomon; documented). `RecoveryMessage` (`ack`/`requestLTRRefresh`/`requestIDR`) + `RecoveryPolicy` model the client→host LTR-refresh-then-2-RTT-IDR-fallback (LTR encode wiring lives in the host). `CursorUpdate` = the <64-byte cursor side-channel (36 bytes: shapeID + visible + host-window-point position + hotspot, doc 17 §3.3). `WindowGeometryMessage` (move/resize/bounds/title, doc 17 §3.8). `InputEvent` (mouseMove/down/up/scroll/key/text, doc 17 §3.9) carrying **normalised (0..1) window coords** + a self-inject `tag`. `CoordinateMapping` (doc 18 §B SOLVED, doc 05 §2): `normalized → host-window-point` in CG top-left space **with NO Y flip** for the click; the multi-monitor fix = flip the window rect into Cocoa bottom-left (`cocoaY = primaryHeight - y - h`) BEFORE intersecting `NSScreen.frame` to pick the right screen's `backingScaleFactor`; a documented pixel-path helper divides by scale ONCE (no double-apply). **(B) `AislopdeskVideoHost` (macOS-only via `#if os(macOS)`; COMPILED + REVIEWED).** `WindowCapturer` = `SCStream` + `SCContentFilter(desktopIndependentWindow:)`, `pixelFormat=420YpCbCr8BiPlanarVideoRange` (NV12 zero-copy), `showsCursor=false`+`showMouseClicks=false` (note: the SDK property is `showMouseClicks`, macOS 15+), `minimumFrameInterval=CMTime(1,30)`, `queueDepth=3`, idle-skip on `SCStreamFrameInfo.status != .complete`, first-frame + ~1s heartbeat IDR, releases the `CMSampleBuffer` after handing off the `CVPixelBuffer`. `VideoEncoder` = the 2-session design built to the EXACT `encode-decode-bench.swift` config — Session A live: spec keys `EnableLowLatencyRateControl`+`RequireHardwareAcceleratedVideoEncoder`; props `RealTime`,`ExpectedFrameRate=30`,`PrioritizeEncodingSpeedOverQuality`,`AllowFrameReordering=false`,`MaxKeyFrameInterval=Int32.max`,`AverageBitRate=12_000_000`+`DataRateLimits=[12_000_000/8,1.0]` (12 Mbps, **/8 not /4**),`SpatialAdaptiveQPLevel=Disable` (macOS 15+ wrapped, as `CFNumber`), ProfileLevel OMITTED, does NOT query `UsingHardwareAcceleratedVideoEncoder` (returns -12900 in low-latency), retries -12905 create-race with backoff; Session B crisp: `Quality=1.0`+`AllowTemporalCompression=false` all-intra + `kVTProfileLevel_HEVC_Main_AutoLevel` (NO `Lossless` key — -12900). `CursorSampler` (~120 Hz `NSEvent`/`NSCursor` → side-channel, main-thread AppKit reads), `WindowGeometryWatcher` (AX move/resize + `CGWindowListCopyWindowInfo` drag-poll), `InputInjector` (activate-then-control: AX `kAXRaiseAction`+set `kAXFocusedWindow`+`NSRunningApplication.activate` → `CGEvent.post(.cghidEventTap)`/`CGWarpMouseCursorPosition`, mapped via `CoordinateMapping`, stamping `eventSourceUserData=tag` for self-inject filtering; Unicode text path; TCC needs — Accessibility + Post Event + Screen Recording — documented in the type). **(C) `AislopdeskVideoClient` (macOS + iOS; COMPILED + REVIEWED).** `VideoDecoder` = `VTDecompressionSession`, `decodeFlags=[]` (synchronous single-frame, MEASURED 0.9-1.1ms), `RequireHardwareAcceleratedVideoDecoder` gated by the type's `@available(iOS 17)`, wraps reassembled AVCC in a `CMSampleBuffer`, outputs NV12 `CVPixelBuffer`. `MetalVideoRenderer` = `CVMetalTextureCache` zero-copy NV12→Metal (luma R8 + chroma RG8) + inline BT.709 YCbCr→RGB shader → `CAMetalLayer` (`maximumDrawableCount=2`); NOT `AVSampleBufferDisplayLayer` (doc 18 §F). `FramePacer` = VSync-driven newest-wins queue (show-last-frame on empty, skip-late — pure logic, the `CADisplayLink` is GUI-only). `ClientCursorCompositor` = side-channel cursor as a `CALayer` over the decoded frame at refresh (pointer latency = RTT, doc 17 §3.3; pure `layerFrame` placement math + hotspot subtraction). `VideoWindowView` = SwiftUI host wrapping a `CAMetalLayer`-backed `NSView`/`UIView`. **(Wiring)** Package.swift: `AislopdeskVideoProtocol` (cross-platform) + `AislopdeskVideoHost` (macOS-only file gating) + `AislopdeskVideoClient` (macOS+iOS) products/targets + `AislopdeskVideoProtocolTests`. `AislopdeskClientUI` gains a documented, gated `VideoWindowFactory` seam (mirrors `TerminalRendererFactory`): the GUI app injects a `VideoWindowView`; absent that (no host capturing) a `RemoteWindowPlaceholderView` explains the secondary GUI path is idle — `AislopdeskClientUI` does NOT depend on `AislopdeskVideoClient`, keeping VideoToolbox/Metal out of the headless build. **(Tests, +44 → 275 total, all PURE.)** packetize/reassemble round-trip (single/multi-fragment, wire encode-decode, reorder, monotonic seq, zero-byte frame, stale dup); **fragment loss → frame dropped + recovery signaled** (no-FEC and FEC-cannot-recover both verified via `nextDroppedFrame()`); **FEC REAL single-loss recovery** (per-group, differently-sized fragments byte-exact, 2-in-group unrecoverable, end-to-end through the reassembler); cursor codec round-trip + **<64-byte size assert** (36 bytes); coordinate mapping (single + multi-monitor Cocoa-flip-picks-Retina-2x + largest-overlap + pixel-divide-once + no-Y-flip corners); window-geometry codec (move/resize/bounds/title incl. CJK/emoji + invalid-UTF-8 reject); input-event codec (all 6 kinds + tag exposure + unknown-type/button reject + modifier bitmask); recovery codec + policy (LTR-first then escalate at 2 RTT); NALU defensive parse. **The reassembler's loss-frontier + drop-queue bug was caught BY the loss tests** (a completed newer frame initially hid the older drop; fixed to surface drops via a queue + a wrap-aware frontier comparison). **VERIFY:** `swift build` compiles ALL targets (incl. host/client video) warning-clean; the iOS triple compiles `AislopdeskVideoProtocol`+`AislopdeskVideoClient`+`AislopdeskClientUI` warning-clean (UIKit `VideoWindowView`, Metal renderer, FramePacer); `swift test` **275/275 green** (231 core + 44 new), 0 failures, warning-clean. No existing public/wire API changed; no terminal wire-format change → `docs/20` unchanged. PATH 1 core stays 231-green.

## Build log — post-WF-10 consolidation session (6 commits on `b4234cd`)

> A follow-up autonomous session on top of the WF-1…WF-10 base (`b4234cd`, 278-green). It
> completes + hardens PATH 1, integrates the PATH 2 video pipeline (still compiled-not-run),
> wires the iOS input responder host + an iOS-triple harness, makes the host the producer of
> the title/bell wire types, and writes (but cannot compile here) the libghostty
> `GhosttyTerminalView` renderer. **End state: `swift build` 0 warnings, `swift test`
> 409 tests / 0 failures, 24 SwiftPM targets (12 lib + 2 exec + 10 test), ~23.6k Swift LOC.**
> Honesty bucket per item is stated explicitly.

- **WF-11 (OS-floor cleanup)** · `42c75cd` · ✅ green · Raised the OS floor to **macOS 26 /
  iOS 26** in `Package.swift` + the app `project.yml`s + `Info.plist`. Removed ~40 now-redundant
  versioned `@available`, collapsed always-true `#available(macOS 15)` branches, dropped the
  non-Darwin `PTYProcess` branches (Darwin-only by design). Doc-only fixes (`External.zig.patch`
  note, README Zig status, `docs/20` strict-v1 wording). No behavioural change; build/tests stay
  green.
- **WF-12 (PATH 1 complete + harden)** · `1a7336a` · ✅ green · **PATH 1 terminal is now a
  complete, hardened end-to-end system (COMPLETE + tested headlessly).** `aislopdesk-hostd --claude`
  selects the curated `ClaudeCodeProfile` launch (`--xterm256` flips `TERM` →
  `xterm-256color`, the #54700 fallback; default `xterm-ghostty`) via `HostdArguments`. Host
  **handshake timeout + reaper** tears down orphaned half-open control channels; an **idle-TTL
  session reaper** tears down the shell/fd/forwarders for long-offline clients. Plain-shell
  `TERM` unified to `xterm-ghostty`. Deleted the orphan `ClientConnection`; renamed misused
  `ClientError.notImplemented` → `invalidState`/`reconnectExhausted`. Fixed the reconnect
  **double-disconnect** by suppressing the self-inflicted `.disconnected` during intentional
  teardown. New tests: `IdleReaperTests` (AislopdeskHostTests), `HostHalfOpenReaperTests`
  (AislopdeskTransportTests), `AislopdeskReconnectSuppressionTests` (AislopdeskClientTests).
- **WF-13 (PATH 2 integrate)** · `cb6b6b9` · ✅ compiled + reviewed / ⚠️ **NOT run** · Wired the
  full PATH 2 pipeline. Host session `AislopdeskVideoHostSession` chains `WindowCapturer` →
  `VideoEncoder` → packetizer → `NWVideoDatagramTransport` (UDP) + input/cursor channels; client
  session `AislopdeskVideoClientSession` chains UDP → `FrameReassembler` → FEC → `VideoDecoder` →
  `FramePacer` → Metal render + cursor composite. `FramePacer` drives CVDisplayLink (macOS) /
  CADisplayLink (iOS). `AislopdeskVideoClient` is now linked into **both** app targets;
  `VideoWindowFactory.shared` is registered in `AppMain`. Bug fixes (never-run-but-reviewed):
  dedicated `VideoChannel.recovery` (5) so a `RecoveryMessage` no longer collides with an
  `InputEvent` on `.input`; `MetalVideoRenderer` pins `CVMetalTexture` wrappers through GPU
  completion + flushes the cache (no use-after-free); `VideoEncoder` throws on latency-critical
  property failures; host `inject()` awaits the activate-then-control raise before posting;
  client recovery escalation (2-RTT LTR→IDR) wired. `docs/20` gained the PATH 2 UDP transport
  section incl. the recovery channel. **HONESTY:** new pure-logic test targets
  (`AislopdeskVideoHostTests`/`AislopdeskVideoClientTests`) cover the state machines / routers / codecs /
  scale math only — NO `SCStream`/`VTCompressionSession`/`VTDecompressionSession`/Metal/socket is
  instantiated in any test (hang rule). **The live decode pipeline is NOT started in the app:**
  `AppMain` registers `VideoWindowView(title:)` whose `connection` is `nil`, so the orchestrator
  never comes up until a host endpoint (`VideoWindowView(title:connection:)`) is wired.
- **WF-14 (iOS responder host)** · `fa79874` · ✅ compiles for iOS + reviewed / ⚠️ on-device
  **unverified** · `TerminalInputHost` — a `UIResponder`/`UIViewRepresentable` host — assembles
  `KeyRepeater` + `KeyboardAccessoryBar` + `IMEProxyTextView` + `FloatingCursorController` and
  routes to `AislopdeskClient.sendInput`. `InputBarView` uses the host on iOS; the macOS path is
  unchanged. Added `scripts/check-ios.sh`: an iOS-triple `xcodebuild` of `ClientApp-iOS`
  (+ `AislopdeskClientUI`) so the `#if os(iOS)` surface is compiled by an automated path. **HONESTY:**
  the host compiles for iOS and is reviewed; the on-device key-repeat / IME / floating-cursor
  interaction has not been run on a simulator/device.
- **WF-15 (title/bell producer)** · `4d14431` · ✅ green · `HostTitleBellSniffer` — a
  non-destructive streaming sniffer on the host output relay — now emits `sendControl(.title)`
  (OSC 0/2, BEL-vs-OSC-terminator disambiguation, bounded buffer with resync, ST + chunk-split
  handling) and `sendControl(.bell)` (standalone BEL). **Wire types 21/22 are now produced
  end-to-end** (previously only decodable). `docs/20` + `docs/21` updated to say title/bell are
  produced. New tests: `HostTitleBellSnifferTests` (+326) and title/bell `AislopdeskClientE2ETests`
  (+44).
- **WF-16 (libghostty renderer wire)** · `46416ce` · ✅ infra / ⛔ **compiled by NO build here** ·
  `GhosttyTerminalView` (a NS/UIViewRepresentable hosting a Metal-backed `GhosttySurface`) is
  written against the libghostty binding and gated behind `#if canImport(CGhostty)`. `AppMain`
  activates `TerminalRendererFactory.shared` when `CGhostty` is present, else shows the
  `BuildStatusPlaceholder`. `docs/21` carries the exact steps to wire the xcframework (built on
  CI / ≤15-SDK host) and flip the renderer live. **HONESTY:** gated/inert on this macOS-26.5
  host (xcframework absent) — verified by **review** against the binding, **not by compilation**.
  The Zig 0.16 `std.Io` rewrite makes porting the fork forward an upstream-scale job (see the
  WF-5 entry + `docs/21` blocker section), so the renderer ships unproven against a compiler
  here.
