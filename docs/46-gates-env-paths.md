# 46 — Gates, env flags, three paths

Full detail split out of `CLAUDE.md` to keep that file small. `CLAUDE.md` carries the one-line rules; this file carries the *why* and the exact conditions. Read the relevant row before choosing a gate, touching a transport, or adding a `SLOPDESK_*` flag.

## Gates — which one reaches which path

Clean checkout builds headless with no prerequisite: `swift build`/`swift test` reach VideoToolbox / ScreenCaptureKit only as SDK frameworks `Package.swift` links on macOS (the calls are Rust's — `slopdesk-apple-vt`, `slopdesk-apple-sck` — behind `CSlopDeskFFI`). The terminal engine is `rust/slopdesk-vterm` over `libghostty-vt`, whose sources are pinned in `ThirdParty/tools/tools.lock` and fetched by `just provision`; it reaches Swift through `CSlopDeskFFI` like every other crate, so the renderer behind the `TerminalSurface` seam compiles under a plain `swift build` and no Xcode-only framework step stands between a checkout and a green core (`docs/68-terminal-surface-in-rust.md`).

| Gate | When / what it uniquely covers |
|------|-------------------------------|
| `slopdesk-gate test-touched` (`just test-touched`) | **Default inner-loop gate after Swift edits** — incremental build + only the test targets whose dependency closure reaches the changed files (~10-50s vs ~100s full `swift test --parallel`). Diffs against the last full-green tree, so scope grows until a full run resets it; Package.swift/golden/unattributable paths escalate to full; a partial green never warms the pre-push cache. Full suite (`just test`) before push / after big cross-cutting changes |
| `just loopback-validate` (`rust/slopdesk-loopback-validate`, `--smoke`/`--frames N`) | FEC / packetizer / reassembler / controller changes — real VT encode→decode, no GUI, no grant. The bare run also drives the closed-loop suite (ABR, adaptive FEC, pacer depth, recovery-IDR, delay-gradient, fps governor, recovery redundancy) and every pure controller; the standalone `--closed-loop`, `--ack-ref`, `--recovery-idr`, `--gradient`, `--pacer-depth` and `--recovery-loss` run one component each. Nothing here reads a clock or a generator, so a verdict that moved is a behaviour that moved |
| `slopdesk-gate ios` | `#if os(iOS)` / UIKit changes — type-checks the iOS slice (`swift build` skips iOS); runs zero tests. CONTENT-STAMPED (`.build/check-ios.sha256`, repo-RELATIVE paths): an xcodebuild over this package graph costs ~85 s whether or not a compiled byte changed — with a private `-derivedDataPath`, a second run on an untouched tree emits an EMPTY build-timing summary and still spends the 85 s resolving packages and re-creating the build description, so PLANNING is the cost and nothing on this side can skip it. One scheme, not two: the `SlopDeskClientUI` scheme this also built is a strict subset of `ClientApp-iOS`'s graph (which depends on it, and through it on all seventeen library targets), so it compiled nothing new for ~85 s an edit. The stamp covers every input the iOS triple compiles — `Sources/`, `Apps/` (spec included), `Package.swift`, `Package.resolved`, the C surface under `ThirdParty/slopdesk-ffi`, and the gate's own source — and only a GREEN run writes it. The Rust SOURCES are deliberately out: a crate body cannot change what type-checks past a C header, and the one Rust change that can (deleting or re-signing a door) moves the header, which IS in the stamp. `--force` re-runs regardless |
| `slopdesk-gate ios-bundle` (`just check-ios-bundle`) | BUILDS the iOS test bundle, which no other gate compiles (`swift build` never sees `Apps/`; `swift test` compiles the macOS branch of every `#if os(iOS)` fork). It went unbuildable for weeks with every gate green, which is why it is gated — but in `check` and NOT in `quick`, on the measurement in "The inner loop" below. Same input set as `ios`, SEPARATE stamp (`.build/check-ios-bundle.sha256`) so a per-edit `quick` cannot mark the bundle checked. `--force` re-runs regardless |
| `slopdesk-gate ios-tests` (`just check-ios-tests`) | ONLY executor of iOS tests (host-less bundle, booted simulator, iOS triple). `swift test` compiles the **macOS** side of every `#if os(iOS)` fork — an iOS default asserted there is asserted about the wrong branch |
| `slopdesk-guigate launch-restore` (`just gui-launch-restore`) | ONLY gate on the shipping launch path (`workspace.json` restore → offer → `connectIfSavedTarget()`). Every other GUI gate sets `SLOPDESK_AUTOCONNECT_*` → `hasAutomationEnvironment()` true → automation branch (persistence nil, layout replaced by one synthetic pane). Run after `WorkspaceStore` restore/autosave, `connectIfSavedTarget()`, or `runArmedLaunchAdoptIfPossible` changes |
| `slopdesk-ops herdr-sync` | `rust/slopdesk-screend` detect-engine or `manifests/*.toml` changes, or a herdr upstream sync — builds the real herdr binary, diffs both engines on ~10k screens (`slopdesk-herdr differential` in `rust/slopdesk-devtools`, driving `slopdesk-screend explain`); pin = `scripts/herdr.pin`. ⚠️ Parity is NOT the goal for every RULE — `DIVERGED_RULES` in that crate names the ones we deliberately made better (today claude's `live_prompt_box` + `legacy_no_prompt_blocker`); every other rule of those agents stays under test, and a mismatch is excused only when a diverged rule explains it. Adding one needs a written reason + a test (`docs/50` §9) |
| `slopdesk-gate android` | ONLY gate on the Android panel's SOCKETS — needs a device in state `device`, an `adb` and a `scrcpy-server` jar (both vendored — `just provision`); sets `SLOPDESK_ANDROID_HW=1`, without which every case in `rust/slopdesk-androidd/tests/hardware.rs` prints why it proved nothing and passes and a clean checkout stays green. Everything PURE about the panel (stream reassembly, control encoding, layout, scroll machine, logcat parse, bridge framing) is already in `just test-touched`; this proves the scrcpy v4.1 handshake and the bridge's line-JSON-then-bytes framing against real `adb`. It also EXPORTS the `adb` it resolved (`SLOPDESK_ADB_BIN`), so the daemon under test uses the same one production would rather than whatever is on `PATH`. Dialect + traps: `docs/48-android-panel.md` |
| `slopdesk-guigate macos`, `slopdesk-guigate video` (`just gui-macos` / `just gui-video`) | GUI proof; needs unlocked Aqua + Screen Recording TCC + Accessibility for the terminal (not over SSH). `video` is also the ONLY proof of the in-place encoder resize (`SLOPDESK_INPLACE_RESIZE`, default on): it drags the client's remote window through System Events and reads the host's swap line, the absence of a restart line, and the client's size adoption plus a still-climbing decode counter off the logs, each counted from before the drag (`docs/70` §2.7). `video --second-client` = ONLY gate with two `SCStream`s + two `VTCompressionSession`s on one capture target (unit tests may not build any of the four). Client B gets the TERMINAL autoconnect only, so it must learn the pane from the document; asserts A's decode counter still climbs after B joins (fan-out, not takeover); screenshots each instance raised by PID — a name-matched raise photographs one client twice |
| `slopdesk-guigate multiclient` (`just gui-multiclient`) | Workspace-document / intent / projection changes — two app instances on one hostd, real menu gesture on one, `slopdesk --socket` reads the other's projection; needs **Accessibility** TCC. Step 7b asserts PTY fan-out unconditionally — every pane in the final layout must take a second subscriber |
| `slopdesk-ops soak` | Fan-out / subscriber-set / out-FIFO / queue-gate / ReplayBuffer-retention changes — real hostd + clients + PTY, laggard frozen with `SIGSTOP`. Asserts retention loses nothing, eviction takes the LAGGARD not the session, the fast member is never head-of-lined, a pane shrunk back to one member still backpressures the PTY. ~80 s, no GUI/TCC. Default `SLOPDESK_SUB_LAG_BYTES` = 4 MiB for speed; `33554432` soaks the shipped value |
| `rust/slopdesk-invariants` (`just lint-invariants`, inside `just lint`) | ONLY gate on the hostd ↔ `slopdesk-superd` CONTRACT. A protocol's two ENDS are the one exemption to the one-implementation rule, and the price is a class of drift neither language's suite can see, because each side stays internally consistent: the rendezvous socket NAME (the two never exchange it — hostd has to *find* the control socket to say `hello`, so a rename is silence, not an error), the protocol major/minor, every verb Swift can send, the listener KINDS (equal in both directions here, unlike verbs: a kind only Rust knows is a socket nobody claims, and an unclaimed socket is never advertised into a child's env — so a `claude` silently comes up with no hook path), the three frame tags, the body cap, and `readChunkSize` ≡ `pump::READ_CHUNK_BYTES` (a joint decision — the Swift copy is what the bounded-queue sizing is reasoned against). Also ratchets three absences and one presence: no pid in ANY unix socket path under `Sources/`, not just superd's three (`docs/51` §1 — the code-panel bridge carried `getpid()` until 2026-08-11, and a backend that now survives a restart would dial a dead address forever); nothing in `Sources/` reading a PTY master (superd owns `read`; hostd's duplicate is the SAME open file description, so a second reader steals rather than observes); no `panels.(code|simulator|android).shutdown()` in `rust/slopdesk-hostd/src/main.rs` — a daemon stop must `relinquish()`, and both spellings compile (`docs/51` §6.7; the rule is `host_owes_superd` in `rust/slopdesk-invariants/src/rules/superd_bodies.rs`, and it read `HostServer.swift` until `docs/60` F.9 deleted it). Text-only, sub-second, no daemon — one tree walk, 300-odd rules under rayon, each with a break-test that seeds the drift and asserts the rule fires. `just supervisor-tests` (`slopdesk-gate supervisor-tests`) adds the five sidecar `cargo test` suites plus the Swift suites that drive a real daemon. Since 2026-08-12 it covers the SECOND two-ended protocol the same way (`docs/52`): screend's rendezvous socket name, its verb BYTES (scoped to `ScreenVerb` — `ScreenStatus` spells `case ok = 0` too), its status bytes by number (the two ends name the failure differently on purpose), the hello banner, and the absence of a revived Swift `TerminalScreenModel`/`TerminalSnapshotRenderer`/`LineOverprintCollapser`. §10 adds the THIRD (`docs/53`), whose ends are further apart than any other pair — a client build shipped months ago is one of them: dropd's request type bytes (each one `client.rs` encodes must have a decode arm) and its reply type bytes (equal both ways — a reply the client cannot decode kills an upload mid-flight), the version, the frame cap, the transfer cap, the announce line hostd re-learns the port from, and the absence of a revived Swift `FileTransferServer`/`FileReceiveLogic`/`FileDropSink`/`FileNameSanitizer`/`LoopbackFileTransferChannel` (DECLARATIONS only, so the prose explaining where they went does not fail its own gate). §11 adds the FOURTH (`docs/48`), whose wire is line-JSON rather than binary, so a skew is not a misparsed length but a request answered `bad request` or a field the panel renders as absent: every `op` string the client can send must have an arm in androidd's `server.rs`, every device-row field it decodes must be one `protocol.rs` encodes, the announce marker must match, and no Swift Android bridge may have come back (`AndroidBridgeServer`/`AndroidToolchain`/`AndroidScrcpySession`/`AndroidDeviceCatalog`/`AndroidEmulatorConsole`/`AndroidSocket`/`AndroidBridgeManager` — `AndroidDevice` is deliberately NOT in that list, being the client END of the protocol). `HostAndroidPerformer` also joined the `relinquish()` ratchet, its bridge having become a held child. §12 adds the FIFTH (`docs/54`), the most silent of them all because BOTH halves are tolerant by design — an unknown tag is skipped and a bad event body is skipped, precisely so one rogue frame cannot end a session's feed, which means a skewed tag errors nowhere and the panel just stays empty: the three frame tags (`1` event and `2` keep-alive written by the daemon, `3` subscribe written by Swift), the 16 MiB cap, the announce marker, and the absence of a revived Swift `TranscriptParser`/`TranscriptTailer`/`TranscriptLine`/`LineAccumulator`/`SubagentWatcher`/`EventBuilder`/`InspectorEngine`/`InspectorReplayLog`/`InspectorSource`/`InspectorServer` |
| `just lint-rust` / `just hook-test` / `just superd-test` / `just screend-test` / `just dropd-test` / `just androidd-test` / `just inspectord-test` | ONLY gates on the Rust side. `just lint-rust` sweeps every workspace `RUST_WORKSPACES` names — the root and every crate `exclude`d from it, 78 of them today, derived by that variable's own grep rather than listed, which `--workspace` does NOT reach, so each needs its own invocation and forgetting one is a silently unlinted crate; the `-test` targets are one per crate. (`slopdesk-hook` is not among them: it is a root-workspace MEMBER carrying `[lints] workspace = true`, so `cd rust && cargo clippy --workspace` already covers it.) `swift build`/`swift test` never compile any of them, so a Swift-only run is blind to the lot. clippy runs `all`+`pedantic`+`nursery`+`cargo` plus a curated `restriction` slice, every group DENY, with `-D warnings` on top, in each workspace; every one of them is `unsafe_code = "forbid"` except the five crates `CLAUDE.md` names, and `slopdesk-invariants` fails on a manifest that is neither exempt nor `forbid` nor inheriting the root's. superd used to be the exception, for the fork/exec window — that window is `rust/slopdesk-posix` now, and a stray `libc::fork` anywhere else is its own failure. ⚠️ `cargo fmt` needs **nightly** (`rust/rustfmt.toml` enables unstable options, and subdirectories inherit it) — the builds and tests are stable-only. That nightly is the **LATEST**, always: `fmt-rust` and `lint-rust` ask for the floating `+nightly`, the prek `rustfmt (apply)` hook inherits it by entering `just fmt-rust` rather than naming a toolchain, `just install-tools` installs-or-updates it, and `lint-invariants`' `nightly-is-never-pinned-to-a-date` fires on any `nightly-YYYY-MM-DD` in the justfile or `.pre-commit-config.yaml`. Know the price before it surprises you: `wrap_comments` decides where a comment BREAKS and that judgement changes between nightlies, so the day rustup fetches a new one this gate is red on files nobody touched (2026-08-30: 3123 lines, 215 files, zero code change). It is a REFORMAT TO TAKE, not a bug — `just fmt-rust`, commit it on its own. A date pin would stop it and buy something worse: every gate agreeing with a rustfmt from months ago, with the bump deferred until it is a thousand-file commit nobody wants to review. The relay's framing is a wire contract with `AgentHookRecord.split`, so `cargo test` is its golden pin. Two paths reach it without being asked: `just test` runs `hook-test` FIRST (warm ~0.07 s, and the pre-push green-tree cache keys on the Swift inputs alone, so it would otherwise skip a rust-only change entirely), and prek gates `cargo test` + clippy on any `rust/**.rs|toml` commit. `just lint` already includes `lint-rust`, so `just check` covers the crate end to end |
| `just lint-deps` (`cargo deny`, inside `just check`) | ONLY gate on what the 78 lock files RESOLVE TO, as opposed to what they build: RustSec advisories and yanked crates, the licence allow list (permissive only — a copyleft or unknown licence fails until it is added to `rust/deny.toml` by hand, with its reason), no `*` version requirement anywhere, and sources limited to crates.io plus the one git pin the tree owns, which `required-git-spec = "rev"` keeps a `rev` rather than a branch. One policy file for every workspace, reached with `--config rust/deny.toml`, because 78 copies would drift the way 78 lint tables did before `lint-floor-agrees`. The advisory database is fetched once and the sweep runs `--offline` (~8 s for the lot; 78 refreshes in parallel was 1 min 43 s). In `check` and not `quick`: what it reads changes at a `Cargo.toml` or `Cargo.lock` edit, not at every one. `docs/69` is the audit the policy encodes |
| `slopdesk-ops restart-hostd` (`just host-restart` / `just host-status`) | Not a gate — the host EDIT LOOP, listed here because it is the command to reach for instead of `pkill`. Builds the daemon with `cargo build` in `rust/slopdesk-hostd` — its own workspace, so a `cd` and not a `-p` — in the configuration the launch record names, SIGTERMs the recorded pid, waits for the process **and** the port, relaunches with the binary/argv/cwd/`SLOPDESK_*` the daemon itself recorded (`hostd-launch.json`, written once the BOUND port is known — `--port 0` mints a different one), and waits for a real listener. Builds before it stops, so a failed build leaves the running daemon alone; confirms the pid via `lsof -d txt` rather than `ps -o comm=`, because `comm` is argv[0] as typed and a recycled pid must never be signalled. Prints the downtime (~0.2 s) and superd's child count either side of it. `--status` changes nothing; `--stop` skips the build. REFUSES, in words, a launch record still naming a `.build/` binary: that is a daemon from before the stage-F cutover, and building the cargo one before replaying that record would report a fresh build while starting the old process. `docs/51` §9, `docs/60` §F |

### The four `slopdesk-guigate` modes own a `slopdesk-superd` each, and census THAT

A GUI gate starts its own supervisor on `$SLOPDESK_SUPERD_DIR=/tmp/slopdesk-gate-<port>-sd` and `SIGKILL`s it on the way out. Not hygiene — the developer's superd is a `LaunchAgent` holding every live pane on the machine, and sharing it made two gates permanently red on any machine that had run them before: `launch-restore`'s fixture names FIXED pane uuids (its seeded `workspace.json` has to), every gate run ends in `SIGKILL` by design, so the panes stayed supervised and the next run's first dial was answered `refused("pane 1111… is already supervised")` — reported by the gate as "the host spawned 0 shells", which is true and points at the wrong half. Each run also leaked its shells into that daemon (nine were parked there when this was found). A private superd starts with an empty registry, takes its panes down with it, and — the third reason — has a pid the gate KNOWS rather than one it would have to `pgrep` for on a machine with a real one running.

That pid matters because **a pane census is taken of superd, never of hostd**: superd forks and holds every pane (`docs/51` §1), so hostd's children are `slopdesk-screend` and a scattering of transient probes, and the pty count these gates took of hostd was structurally zero against a host whose own log said it had attached three shells. Session-leadership alone is no longer the whole discriminator either, now that the parent is right: a SERVICE pane (`code-server`, `slopdesk-dropd`, `slopdesk-androidd`) is a real pane on a real pty, so `argv[0]`'s login-shell `-` separates the two with no table of service names to keep in step.

## The inner loop — `just quick` after every edit, `just check` before pushing

`check` = `lint lint-deps build test miri golden check-ios check-ios-bundle check-macos-apps`. `quick` = `ffi lint test-touched golden check-ios check-macos-apps`: the same gate with the full suite swapped for the touched-target selection, and TWO omissions — Miri (a memory audit answers a question only an `unsafe` edit can ask, and the three crates that may write it are each covered by `just lint-rust` and their own differential suites) and `check-ios-bundle`.

**`check-ios-bundle` is the one omission that is about COST, not about what an edit can break** — and it is the reason to read this section rather than assume it. Building `Apps/ClientApp-iOS/Tests` used to happen inside `check-ios`, which put it after every edit. Measured 2026-08-30: a `quick` whose iOS stamp missed took **41 minutes**, of which that single `xcodebuild … build-for-testing` was **25+**, at 94% of ONE core out of ten. Warm, the whole of `quick` is **72 s**. The miss is not exotic — the stamp covers the closure of the products the iOS spec names, so `SlopDeskWorkspaceCore`, `SlopDeskClientCore`, `SlopDeskPhoneUI` and the FFI header all pay it.

The cost is structural, not a misconfiguration: the test target carries `SWIFT_ENABLE_TESTABILITY: YES` and the app target does not, so the two builds are different configurations and share no compiled module — the package graph compiles a second time for seven test files. Five of those seven `@testable import`, so the setting cannot come off. Only the RATE was available, so the bundle build has its own verb (`slopdesk-gate ios-bundle`), its own stamp (`.build/check-ios-bundle.sha256`) and its own recipe, and it runs before a push instead of after a keystroke. Two stamps rather than one on purpose: a shared stamp would let a `quick` that built only the app record "iOS inputs checked" and let the pre-push `check` skip a bundle it never built.

## ⛔️ THE CARGO TARGET DIRECTORIES LIVE OUTSIDE THE CHECKOUT, AND IT IS THE INNER LOOP'S BIGGEST SINGLE COST

Both app specs declare the SwiftPM package as `path: ../..` — the **repo root**. Xcode enumerates that
whole tree on every invocation, single-threaded, one `lstat` per file
(`IDEContainer _locateFileReferencesRecursivelyInGroup:` → `IDESwiftPackageAbstractGroup.expectedSubitemPathsToTypes()`
→ `DVTFilePath _locked_vnodeKnownDoesNotExist:`). Measured 2026-08-31, both directions, with `sample`
naming the frames:

| `rust/**/target` | files under the package root | `slopdesk-gate ios --force`, no source change |
| --- | --- | --- |
| in place | 3,108,486 | **987 s** |
| moved to a sibling tree | 1,499 | **22 s** |

⚠️ **An `xcodebuild` with no compiler children and no derived-data writes is not building — `sample` it
before assuming it is.** The 987 s run compiled nothing at all.

The layout has two halves and both are ratcheted by `lint-invariants`
(`targets-outside-the-checkout`):

- **a `.cargo/config.toml` per crate directory**, committed, plus the root workspace's. This is
  the half that makes cargo WRITE outside. It is a RELATIVE `target-dir` into a `slopdesk-targets`
  sibling of the checkout, never an absolute one — the file is committed, so an absolute path would
  bake this machine's checkout location into the repository. ⚠️ **cargo discovers config from the CWD,
  not from the manifest**: `cargo --manifest-path rust/X/Cargo.toml` run from the repo root reads
  `rust/.cargo/config.toml` and not the crate's, which is why the root workspace's config is required
  rather than optional. Root-workspace MEMBERS are exempt and the rule derives that from `members`
  rather than a list — a member builds into `rust/target` and owns no directory of its own.
- **each crate's `target`**, a gitignored SYMLINK, rebuilt by **`just relink-targets`** (idempotent).
  This is the READ half: ten production files, three justfile sites and one Swift test resolve a daemon as
  `<crate>/target/release/<name>`, and `cargo clean` removes the link rather than its contents. Run
  the recipe after a fresh clone and after any `cargo clean` that took a link out.

**The symlinks do not undo the win, and that is the surprising half** — an earlier round recorded the
opposite and was wrong. Xcode's walk uses `lstat`, which does not follow a symlink, so the link is a
leaf to the enumerator and a directory to everything else. Re-measured five ways with the links live:
`-showBuildSettings` 15–29 s, a full `ios --force` 31 s.

**The general rule, which is what to remember: the walk skips DOT-directories and walks every other
one.** That is why the package root can still hold 552 K files and cost nothing — `.build` (159 K),
`.work` (298 K), `.git` and, when this was measured, the ghostty fork's two dot-dirs (62 K) are all
invisible to it, and only ~3,400 non-dot files are left. Measured directly: seeding 200 K files into a NON-dot directory
at the root took the same probe from **15 s to 54 s**, and removing it put it back. So the invariant
worth holding is not "keep cargo out" but **any large generated tree under the repo root must be
either dot-prefixed or outside the checkout**. `slopdesk-targets` is outside; `.build` and `.work`
are dotted; a future scratch tree that is neither would cost the inner loop directly, and it is the
kind of regression nothing else notices because everything still compiles.

It is cheap because nothing in it re-does work whose inputs did not move:

- **`slopdesk-gate ffi`** hashes the shim's sources and every crate reachable from it by a `path = "../…"` edge, plus the header and the script itself. A second run on a warm tree exits in milliseconds; `--check` (in `just lint`) reports staleness without building. Its three slices build CONCURRENTLY, each into its own directory under the shim crate's derived `target/ffi` — separate directories rather than one, because cargo takes an exclusive lock on a target directory and three builds sharing one merely queue. Measured on one edit to a wrapped crate: 70 s serial, 55 s backgrounded onto the shared directory, **14 s** as it stands. The header⇄library symbol check on each slice is two `comm` passes over sorted sets, not 776 `grep -c` sweeps per slice (which cost 20 s and, being substring matches, let a door renamed to a longer name pass).
- **`slopdesk-gate ios`** is stamped as the table row above describes: **0 s** when no iOS-compiled input moved. A Swift edit does move it, and then the gate costs what one xcodebuild plan costs — which is why `quick` runs it CONCURRENTLY with `test-touched`, the other cost a Swift edit pays. Those two share nothing but the SwiftPM lock, and the only thing that lock delays is `golden`.
- **A `.swift` or a `.h` is stamped as CODE, not as bytes** — `gates/code_text.rs` strips comments and normalises inter-token whitespace before the digest, so a doc-comment edit leaves both Xcode stamps exactly where they were. Measured: a one-word doc edit in `Sources/SlopDeskVideoProtocol`, which sits deep in BOTH app graphs, cost fifteen minutes across the two triples for a change the lexer discards before it parses a declaration. It is a real lexer and not a regex because the only failure that matters is the one direction — classifying CODE as a comment would leave a warm stamp over a live change — so it carries Swift's nesting block comments, raw and multiline literals and interpolation stack, C's char literals, and a canary test that appends a comment plus a statement to EVERY source in the tree and asserts the lexer is not left inside a literal.
- **`just lint`** runs its five linters concurrently into per-linter logs, replayed in the declared order once all have finished — ordered output without interleaving, and without just's `[parallel]` attribute, which does not order its output either. 55 s → **34 s**. It was bounded by the shell ratchet until that became `rust/slopdesk-invariants`; what is left of it is `lint-reach`, four `just --dry-run` expansions, one content stamp and one directory listing. The fourteen repo-wide token bans it used to start as a background Python process are rules in the same crate, and run with the rest of that registry under `lint-invariants`.
- **`test-touched.sh`** selects by dependency closure and escalates to the full suite on any path it cannot attribute — fail-toward-slow.
- **`slopdesk-gate hooks`** is the directory listing: every stage `.pre-commit-config.yaml` declares in `default_install_hook_types` must have a file in this clone's hooks directory (`core.hooksPath` when set, else `git rev-parse --git-path hooks`, which is what makes it right inside a worktree). It costs one `read_dir`. It is on `lint-reach` and NOT on the `pre-push` stage on purpose: `prek install` writes those files once, at the moment it is typed, so a stage added to that list afterwards is dead — and a gate reachable only through a hook would be silent exactly when the hooks are the thing that is missing. `commit-msg` spent 2026-08-11 to 08-31 in that state; see `docs/DECISIONS.md`.
- **`slopdesk-gate corpus`** is the privacy ratchet over the recorded terminal corpus: no COMMITTED `.sdrec` may carry an absolute home path, a key or credential-shaped assignment, or this machine's own user or host name. It reads `git ls-files`, not the working tree, because a recording that was made and then rejected stays on disk and no change to the repository can take it out of somebody's checkout — the committed set is the one a clone receives and the only one a commit can fix. It floors at four files, so a glob that stopped matching reds instead of passing vacuously, and it reports the KIND of fingerprint and never the value, which would copy the leak into the log. It exists because `top` held a corpus slot for one afternoon on 2026-09-02 with the recording machine's process list and its operator's user name 435 times in it, caught by hand before the first commit; the recorder's own half is that `slopdesk-ttyrec` now gives its child `PATH`, `HOME`, `LANG` and `LC_ALL` and nothing else unless `--env` or `--inherit-env` says so. It is on `lint-reach`, with the other questions the tree cannot answer about itself.

The one thing to remember: `quick`'s speed is entirely a claim that a stamp is honest. When a verdict looks wrong, `slopdesk-gate ffi --force` / `slopdesk-gate ios --force` re-run unconditionally, and `just check` is always the answer of record.

Ratchets inside `just lint`: `slopdesk-invariants` rules `design-token-leaks` (Slate tokens live in `Sources/SlopDeskSlate/SlateDesign.swift`) and `menu-shortcutless` (no `.keyboardShortcut` in `WorkspaceCommands.swift`), plus the rest of that registry (row above).

SIMD detail: the frame hash is scalar (`rust/slopdesk-video/src/frame_hash.rs`), **not** NEON — only the GF(2⁸) multiply kernel has one, and it lives alone in `rust/slopdesk-gfsimd`, the third `unsafe` crate. Two suites pin it: `rust/slopdesk-gfsimd/tests/differential.rs` for kernel ≡ scalar twin, and the cross-region cases in `rust/slopdesk-video/src/gf256.rs` for the seam the kernel cannot see on its own — a region whose tail straddles a 16-byte chunk.

Hang-safety detail: video unit tests stay pure `SlopDeskVideoProtocol` + controllers — no `SCStream`, `VTCompressionSession`, `VTDecompressionSession` or Metal device. Security detail: there is no app-layer crypto/auth; the replay buffer holds raw bytes and the WireGuard mesh is the boundary.

## Three paths (do not merge)

Separate transport, message set, version (`1` only — no negotiation).

| Path | Notes that bite |
|------|-----------------|
| Terminal (TCP) | Dual `.data` + `.control`; `TCP_NODELAY` on **both**. ReplayBuffer 256 MiB cap, 64 MiB offline gate **pauses PTY drain**; queue gate 64 KiB attached (latency) ↔ 64 MiB detached (budget — agent keeps running while away). Both pauses now travel one hop further: the drain is `slopdesk-superd`'s (`docs/51` §6.5), so a pause is a `pause` verb that stops its reads, the kernel PTY buffer fills, and the shell backpressures — same never-drop mechanism, one process away. Real smoke: `SubprocessE2ETests` (in-memory loopback misses open-order races). |
| GUI video (UDP) | Media socket (1-byte channel tags; recovery has its own tag) + dedicated cursor socket. FEC via `FECScheme` (RS GF(2⁸)). |
| Inspector (TCP) | Read-only; client→host is only `subscribe(fromSeq:)`. Served by `slopdesk-inspectord` on `terminalPort + 1`, dialled directly — hostd relays nothing and, since `docs/54`, holds nothing. |

**Not a fourth path (2):** the Claude Code hook relay is a SEPARATE PROCESS (`~/.claude/hooks/slopdesk-agent`, built from `rust/slopdesk-hook`) that speaks one line-framed record over AF_UNIX. superd owns the listener and hands each accepted connection to hostd over `SCM_RIGHTS`; the drain, the routing and the per-pane state machine are `rust/slopdesk-hostd/src/hooks.rs` — no FFI, no `SlopDeskProtocol`, no wire version. It is same-machine by construction (the hook runs inside the host's own PTY). ⚠️ It must stay SYNCHRONOUS (ordering is meaning) and must keep BOTH `PreToolUse` and `PostToolUse` — see the CLAUDE.md invariant. `swift build` does not produce it; `just build` stages it beside the host binary, together with the `slopdesk-agenthooks` that installs it — which is where `AgentHooks.locate` looks.

**Not a fourth path:** the Simulators panel speaks a FOREIGN wire (`baguette serve` — HTTP + one websocket, H.264 down / JSON up) to a third-party process, sharing no socket, message set or codec with the three above; `SlopDeskProtocol` never sees a byte of it. Only discovery (metadata verb 21) rides a SlopDesk wire, and it carries an address, not frames. Dialect, traps and fixtures: **`docs/47-simulator-panel.md`** — read it before touching anything under `Sources/SlopDeskPhoneUI/Panel/Simulator`. Gate: `just test-touched` (the whole panel is unit-testable — both runtime seams are injectable, so no test opens a socket or builds a display layer).

**Nor is the Android panel:** a SECOND foreign wire — the host's own bridge relaying `scrcpy-server` v4.1 (H.264 down / touch and key messages up, on ONE full-duplex TCP connection). The relay is not optional: `adb forward` binds 127.0.0.1 only, so a mesh client cannot reach the device socket without it (`adb -a server -H 0.0.0.0` was rejected — machine-wide, and it hands every peer a device shell). Only discovery (metadata verb 22) rides a SlopDesk wire. Dialect, measurements and traps: **`docs/48-android-panel.md`** — read it before touching anything under `Sources/SlopDeskPhoneUI/Panel/Android` or `rust/slopdesk-androidd`. Since 2026-08-12 the relay is that crate — a separate binary held by superd, dialled DIRECTLY by the client, with the Swift original deleted in the same change. hostd only ensures it (verb 22) and never sees a frame. Gates: `just test-touched` for everything pure on both sides, `slopdesk-gate android` for the sockets, `slopdesk-invariants` for the two-ended constants.

## Env (`SLOPDESK_*`)

Not exhaustive — grep `SLOPDESK_`. **Default idiom:** `!= "0"` → default-ON; `== "1"` → default-OFF; check the call site.

| Flag | Notes |
|------|--------|
| `SLOPDESK_FEC_M` / `_FEC_K` | the Reed-Solomon `[k + m, k]` code's two numbers: `m` parity shards per group of `k` data fragments. Defaults `k` 5 / `m` 1 — the XOR-equivalent that ships, and the mixed-fleet interop baseline. **Set identically host + client, and DEPLOY TOGETHER**: `m` is not a wire field, so each end maps the parity boundary from its OWN configured scheme, and a disagreement is silent in the worst available way — nothing fails to decode, nothing logs, repairs just stop. Resolution, the 1–8 / 2–64 clamps and the GF(2^8) `k + m <= 255` cap are `rust/slopdesk-video`'s `adaptive_fec::multi_loss`, called by BOTH ends so there is no second clamp. Host side: `slopdesk-videohostd`'s `session::configured_fec`, folded from `video.fecM` / `video.fecK` by `env.rs`. Until 2026-08-31 the host ignored both and pinned the default while the client honoured them — that hazard, and its fix, are `docs/61` §4 divergence 5 |
| `SLOPDESK_CONFIG_FILE` | which `config.toml` the app and the CLI read, outranking `$XDG_CONFIG_HOME/slopdesk/` and `~/.config/slopdesk/`. Resolved in `rust/slopdesk-settings`, crossed to Swift as ONE snapshot per launch/reload. It is also what makes ⌘, open the file the app actually honours rather than the default address. Detail: `docs/58-configuration.md` |
| `SLOPDESK_*` via `[env]` | `config.toml`'s `[env]` table sets any of these by raw name. It folds into `EnvConfig.overlay`, so the precedence is the one this table has always had — **real env var → `[env]` overlay → compile-time default** — and it is applied above every typed key the file also sets. A deliberate `SLOPDESK_*=…` on the command line is still the operator's escape hatch and is never overridden by the file |
| `SLOPDESK_VIDEO_DEBUG` | Video stderr |
| `SLOPDESK_DISPLAY_CAPTURE` | `window` / `display` / `include` |
| the CAPTURE operating point (28 keys) | every gate `WindowCapturer` acts on — the encode queue, the pacer, freshest-wins, still/crisp cadence, adaptive QP, idle skip, self-heal, the scroll heuristics. Resolved ONCE through `CaptureGateTable` → `slopdesk_video_capture_gates` → `rust/slopdesk-video`'s `capture_gates`, which owns the key list, the defaults, the clamps and the three conjunctions. **The list is the crate's**, so grep `KEYS` there rather than the Swift. Until 2026-08-26, twenty-five of them read `ProcessInfo` directly and the `[env]` overlay never reached them — a `config.toml` entry for any of the twenty-five did nothing at all. All twenty-eight now go through `EnvConfig`, so the row above about `[env]` applies to them. ABSENT ≠ empty on this path: `SLOPDESK_VIDEO_DEBUG` tests PRESENCE, so `=0` turns it ON |
| `SLOPDESK_PACER` | default present-on-arrival; `=deadline` for smoothness pacer |
| `SLOPDESK_AUDIO` | host app-audio stream gate (default-ON); `_CODEC=pcm` bypasses AAC-ELD |
| `SLOPDESK_AUDIO_CODEC` / `_BITRATE` | read in `slopdesk-video`'s `audio_source`, NOT in Swift — the sender asks `slopdesk_audio_wire_format()` / `slopdesk_audio_bitrate_bps()`. Only the exact word `pcm` picks the codec-free arm (an unknown value is not a codec, so it stays AAC-ELD); the bitrate is CLAMPED to 32–320 kbps, default 128, and a typo parses to nothing rather than to zero. the `video-audio-row` invariant (`just lint-invariants`) bans either name under `Sources` — a Swift `ProcessInfo` read would be a second clamp, and two clamps that must agree cannot be tested for |
| `SLOPDESK_SEND_LANE` | default-ON. `=0` runs the send schedule inline on the session actor instead of on the dedicated lane. Documented as a byte-identical fallback and it was NOT: the inline path had no `keyframe` in scope, so it floored every frame at the delta pace floor — including a recovery IDR, whose delivery time IS the client's recovery time. Both paths now build one schedule (2026-08-22) |
| `SLOPDESK_PACE` | default-ON (`=0` disables). Chunk pacing on the send path |
| `SLOPDESK_PACE_US` | pins a STATIC inter-chunk gap in µs, for A/B against the adaptive one. An explicit value outranks `_PACE_ADAPTIVE` |
| `SLOPDESK_PACE_ADAPTIVE` | default-ON (`=0` reverts to the fixed gap). Derives the gap from the live ABR target rather than a constant |
| `SLOPDESK_PACE_RATE_X` | pace at this MULTIPLE of the live ABR target, default 2.5. TUNING, not a toggle |
| `SLOPDESK_KF_DUP` / `_KF_DUP_LOSS` | default-ON. Duplicates a keyframe's chunks above a loss threshold; the threshold is its own variable because the FEC tier is default-OFF, so gating on the tier alone would leave the default config always-dupping |
| `SLOPDESK_SMALL_DUP` / `_SMALL_DUP_MAX_BYTES` | default-OFF (`=1`). The keyframe gate's twin for DELTAS, and gated far harder because a delta does not buy back a recovery: the frame must be a delta, at or under the byte ceiling (default 1400, no clamp), and the `SLOPDESK_ADAPTIVE_FEC_M` ladder must have stepped OFF its clean rung. That last term is the whole point — it covers the window between "the link started losing" and "the raised parity reaches the wire". On an established clean link that window does not exist, because the ladder has relaxed to its clean rung; a session that has taken no feedback report yet is NOT such a link and does duplicate, since it starts on the baseline rung and has no evidence either way. A not-running ladder is NOT "stepped": `should_dup_small_delta` takes the rung as an `Option`, because `PARITY_TIER_CLEAN` is a rung and comparing a default tier against it would arm this on every small delta forever. One frame gets one second copy or none — this and the keyframe gate feed the SAME verdict |
| `SLOPDESK_IDR_TOKENS` / `_IDR_REFILL_MS` / `_IDR_GRACE_MS` | the recovery-keyframe policy's three tunables, all TUNING rather than toggles — the token bucket's capacity (clamp 1–4), the ms per refilled token (clamp 100–5000, default 500 = the old sent-keyed spacing) and a PIN that sets the in-flight grace floor and ceiling to the same value for an A/B (clamp 0–1000 ms; unset leaves the adaptive `clamp(0.75×smoothedRTT, 40, 250)`). The key list, the defaults and every clamp are `rust/slopdesk-ffi`'s `recovery_idr`, beside the law they tune — the host asks for the names and lends the texts back, so there is no second clamp to keep in agreement. A text that will not parse leaves ONLY its own field at the default. Read through `EnvConfig`, so the `[env]` row above applies; until 2026-08-27 this was the host's last raw `ProcessInfo` read and a `config.toml` entry for any of the three did nothing |
| `SLOPDESK_NACK` / `_NACK_RING_FRAMES` | default-OFF (`=1`). Selective-ARQ retransmit; needs host AND client deployed together. The ring holds 96 frames by default |
| `SLOPDESK_ADAPTIVE_FEC_M` | a SWITCH, default-OFF, on-value the literal `1` — it turns on the PARITY ladder, which steps `m` per report across tiers 5/6/7 (clean/normal/burst). It does NOT carry a parity count; that is `SLOPDESK_FEC_M` above, and reading this one as a threshold (`>= 2`) inverts it at exactly the documented on-value. READ ON BOTH SIDES of a shipped wire — `session_inbound::adaptive_m_enabled` spells it as `AdaptiveFECPolicy.swift` does, because a host that disagreed about what turns the ladder on would step tiers the peer never expects. It outranks `SLOPDESK_ADAPTIVE_FEC`'s group-size ladder: the two are mutually exclusive and a tier means a different thing to each. It is also the ladder term in `SLOPDESK_SMALL_DUP` above, and it decides the SEED: with this on, a fresh session starts at the ladder's own baseline rung (6) rather than at tier 0, so the first frames already stamp a tier the pass-through owns |
| `SLOPDESK_SUB_LAG_BYTES` | laggard-eviction threshold, default **32 MiB** — deliberately BELOW the 64 MiB offline gate. TUNING, not a toggle. A lone subscriber is never evicted because eviction needs two or more members |
| `SLOPDESK_SUPERD_SOCKET` | points hostd at a superd other than the login session's. **Every test that opens a pane must set it**, or it drives the developer's live custodian and a stray `release` ends somebody's running agent. `subprocess_e2e.rs`'s `Sandbox::build` is the ONE constructor for a daemon environment on the client
side: it boots a private superd and sets this alongside the four container variables, and it is the
only way to get a `Command` in that file — the rule the Swift needed a companion source-scanning
suite to police is enforced by construction there. Read by BOTH ends — it is where superd `bind`s as well as where a client `connect`s — so a fixture must state the DAEMON's copy explicitly rather than let it inherit the test process's, which between fixtures deliberately points at a path nothing binds |
| `SLOPDESK_AGENT_CONTROL` | `=1` makes hostd CLAIM the agent-ctl listener. Default-OFF (writing to PTYs and spawning shells is not enabled silently). superd binds that socket either way and knows nothing about this flag — what it decides is whether hostd `listen`s for the `control` kind, and an unclaimed listener is never advertised into a spawned child's env, so off still means no `SLOPDESK_CONTROL_SOCKET` reaches a child (`docs/51` §6.6). The hook listener has NO flag, deliberately (`DECISIONS.md` 2026-07-29) |
| `SLOPDESK_SCREEND_SOCKET` | where `slopdesk-screend` binds and `ScreenClient` connects, default `$TMPDIR/slopdesk-screend.sock`. Read by BOTH ends, like superd's — `slopdesk-screenwire`'s `SOCKET_ENV_KEY` is where each end spells it, so there is one name and not two. Resolved PER CALL rather than captured at construction, because a client that froze the address at startup would freeze whatever the process saw FIRST, and a test that aims a private engine somewhere else afterwards would be talking to the developer's. An EMPTY value means the default and not the empty path: one end used to treat `SLOPDESK_SCREEND_SOCKET=` as an override and the other as unset, which is a disagreement no fixture can see |
| `SLOPDESK_SCREEND_BIN` | which `slopdesk-screend` the client starts when none is listening. Spelled once, as `slopdesk-sidecars`' `SCREEND_BIN_ENV_KEY`. Absent → the installed copy, then the cargo target dirs found by walking UP from the running executable — a SEARCH, not a fixed depth, because a test binary and a `cargo run` binary sit at different depths under the same target dir. Deliberately no `PATH` search: screend is not a user-facing command |
| `SLOPDESK_SCREEND_IDLE_EXIT` | seconds `slopdesk-screend` stays up holding NO connection before exiting, default 120, `0` = never (what the LaunchAgent sets, since launchd owns that copy). Read by screend ITSELF and by nothing that starts it, which is the whole point: an engine is spawned DETACHED, so no harness owns a handle to reap it and its own timer is the only thing that ends it. Two live consumers, and they want opposite ends of the range — the LaunchAgent sets `0` because launchd owns that copy, and `subprocess_e2e.rs`'s sandbox sets **5** because each scenario's hostd starts a private engine and the stock 120 would leave one alive per scenario for two minutes after a run (`docs/52`) |
| `SLOPDESK_FILE_TRANSFER` | `0` turns PATH 4 off entirely — hostd starts no `slopdesk-dropd` and nothing listens on `terminalPort + 2`. Default ON. Read by hostd, and it is the only PATH-4 switch: there is no toggle for the drop UI, which simply fails to connect |
| `SLOPDESK_FILE_DROP_DIR` | where uploads land, default `~/Downloads`. A leading `~`/`~/` is expanded against the DAEMON user's home, not the caller's. hostd resolves it and passes the absolute path on dropd's argv, so the child never re-reads the variable |
| `SLOPDESK_DROPD_BIN` | which `slopdesk-dropd` hostd spawns, and which one `DropdE2ETests` dials. Absent → the installed copy, then the cargo target dirs found by walking UP from the running executable, same search as `SLOPDESK_SCREEND_BIN` and for the same `.build/debug`-is-a-symlink reason |
| `SLOPDESK_SUPERD_DIR` | superd's own: relocates the whole socket directory (control + hook + agent-control + lock). Read once at startup, never per call — a daemon that answered "where is the hook socket?" differently at minute 40 than at minute 0 would hand two children two different addresses, which is `docs/51` §1 wearing a different hat |
| `SLOPDESK_PANE_RING_BYTES` | per-pane output retention in superd, default **4 MiB** — roughly a minute of a full-tilt build, sized for a restart rather than for history. `0` is honoured and means "retain nothing": the offsets still advance, so a returning hostd is told truthfully that what it missed is gone, instead of being handed a silent splice. TUNING, not a toggle |
| `SLOPDESK_WORKSPACE_STATE_DIR` | relocates `workspace-state.json`. EVERY `HostServer` builds a `HostWorkspaceStore` — a test that calls `start()` must set this or inject `workspaceStore:`, or it reads/overwrites the developer's real workspace |
| `SLOPDESK_ADB_BIN` / `_ANDROID_EMULATOR_BIN` / `_ANDROID_SERVER_JAR` / `_ANDROID_EMULATOR_ARGS` | Android panel tool overrides. No `adb` ⇒ the tab is `unavailable`; a missing `emulator` or jar is NOT (a plugged-in phone still lists, and a host with no jar still boots AVDs — it just cannot mirror). `adb` and the jar are vendored (below); the **emulator is deliberately not** — it comes from `sdkmanager` and is useless without gigabyte system images behind a licence accept. Detail: `docs/48-android-panel.md` |
| `SLOPDESK_ANDROID_HW` | `=1` arms `rust/slopdesk-androidd/tests/hardware.rs` (needs a booted device). Set by `slopdesk-gate android`; off ⇒ every case prints why it proved nothing and passes — Rust has no `XCTSkip`, and a silent pass is indistinguishable from a gate that ran and proved nothing |
| `SLOPDESK_HOSTD_BIN` | **No reader.** It named the binary the menu-bar app spawned, and `docs/60` deleted the app — hostd is controlled entirely by CLI. There are two ways to start one now and neither consults a variable: `slopdesk-ops install hostd` writes a `LaunchAgent` naming the copy it installed under `~/Library/Application Support/SlopDesk/bin/`, and `slopdesk-ops restart-hostd` replays a recorded launch path. The row stays because the variable's ABSENCE is the claim: a search order for hostd would be a second one beside the installer's, and there is nothing left to search from |
| `SLOPDESK_E2E_HOSTD_BIN` | which `slopdesk-hostd` `rust/slopdesk-client/tests/subprocess_e2e.rs` starts, and the ONLY reason it is not the row above. Unset ⇒ every scenario prints a skip line naming the variable, which is what `cargo test -p slopdesk-client` does in the ordinary suite; `just client-e2e` builds `host` and `superd` and sets it. A distinct name rather than a reader for `SLOPDESK_HOSTD_BIN`, because the two claims are opposites: nothing in the PRODUCT may search for a hostd, and this is a test naming a binary it built one recipe ago. Reusing the name would make the absence above unprovable |
| `SLOPDESK_INSPECTORD_BIN` | which `slopdesk-inspectord` hostd spawns. Absent → the installed copy, then the cargo target dirs found by walking UP from the running executable, same search as `SLOPDESK_DROPD_BIN`. There is no separate on/off switch: hostd's `--inspector` flag decides whether PATH 3 comes up at all, and `--transcript` names the file the daemon follows |
| `SLOPDESK_ANDROIDD_BIN` | which `slopdesk-androidd` hostd spawns. Absent → the installed copy, then the cargo target dirs found by walking UP from the running executable, same search as `SLOPDESK_DROPD_BIN` |
| `SLOPDESK_PROBE_BIN` / `_CODESEED_BIN` / `_AGENTHOOKS_BIN` | which `slopdesk-probe` (the metadata RPC's git/directory/session half), `slopdesk-codeseed` (the code panel's workbench profile) and `slopdesk-agenthooks` (the Claude Code hooks installer) hostd forks. The probe and the installer are ROOT-workspace binaries, so their cargo target dir is `rust/target/` and not the per-crate one `RustServicePaths` walks for: absent, they resolve BESIDE the running host executable, where `just build` stages them. The seeder has its own workspace and takes the ordinary `RustServicePaths` walk. A missing probe costs the git line and the file tree and nothing else; a missing seeder makes the code panel `unavailable`; a missing installer reports the hooks NOT installed and refuses to install them |
| `SLOPDESK_CODE_SERVER_BIN` | Code panel binary override, and the escape hatch that outranks the pin (bisecting a candidate build). Unset ⇒ the `locate_tool` walk described under **Vendored runtime deps** below |
| `SLOPDESK_BUILD_HASH` | Read at RUNTIME by `slopdesk version` (`CLIVersion.swift`) — appends a short build hash in parentheses. Absent in a plain `swift build`, so the parenthetical simply vanishes |
| `SLOPDESK_VERSION` / `_BUILD_NUMBER` / `_SIGN_IDENTITY` / `_NOTARY_PROFILE` / `_SKIP_NOTARIZE` | `slopdesk-release package` only — never read by shipped code. `_VERSION` is required and must match the `CLIVersion.version` constant or the script refuses to package. `_SKIP_NOTARIZE=1` is a pipeline dry-run: the output is signed but will NOT pass Gatekeeper elsewhere. Detail: `docs/49-release-pipeline.md` |

Deleted deliberately — do not reintroduce: `SLOPDESK_WORKSPACE_DOC`, `SLOPDESK_PANE_FANOUT` (multi-client sync is unconditional; a host that refuses `channelClass 1` gives a client a blank window with no error).

## Vendored runtime deps

The right panel's surfaces stand on programs this repo does not build. They are **pinned by URL + SHA-256 in `ThirdParty/tools/tools.lock`** and provisioned by `rust/slopdesk-provision` (`just provision`, `just provision-check`) into `ThirdParty/tools/.prefix/bin` (gitignored, ~730 MB). Same bargain the lock's fourth kind — `git`, a source tree pinned at a 40-hex commit, which is how the terminal engine arrives (`docs/68` §3) — strikes one row down: the pin is committed, what it fetches is not.

**Why.** Homebrew's `code-server` formula froze at 4.112 and was deprecated, and nothing in the repo recorded — or could enforce — the version the panel was written against. The panel sat on Code 1.112 for months, below the 1.121 floor where the built-in mermaid preview landed, and no gate could see it.

| Dep | Surface | Pinned | Note |
|---|---|---|---|
| `code-server` | code panel (verb 18) | 4.135.0 | floor is **4.121** — Code 1.121 is where `mermaid-markdown-features` became built-in |
| `baguette` | simulator panel (verb 21) | 0.1.97 | executable is `Baguette`, sibling to the `.bundle` it loads assets from; the `bin/` symlink was checked to still resolve it |
| `adb` | Android panel (verb 22) | 37.0.1 | Google's versioned zip; its `repository2-3.xml` SHA-1 was cross-checked against our SHA-256 |
| `scrcpy-server` | Android panel | 4.1 | **committed** at `ThirdParty/tools/vendor/scrcpy-server` (716 KB, not an executable — the device's `app_process` runs it). `slopdesk-provision` verifies those bytes, never downloads them |

**Search order** (`locate_tool` in `rust/slopdesk-androidd/src/toolchain.rs`, reached from Swift through `slopdesk_host_service_binary` — there is no second copy to mirror, and the wording that used to name a Swift `searchDirectories` as the canonical one is why the two drifted: `FileManager.isExecutableFile` is `access(X_OK)`, which is TRUE for a directory, so a directory wearing a tool's name on `PATH` reached `posix_spawn`. The crate tests `is_file()` and the mode bits and walks past it. The Swift `AndroidToolchain` that used to hold the other copy is on the must-stay-deleted list in the row above): `SLOPDESK_*_BIN` override → **vendored prefix** → `PATH` → `~/.local/bin` → `/opt/homebrew/bin` → `/usr/local/bin`. The prefix outranking `PATH` is deliberate and is the whole point: a stale Homebrew copy silently winning is the failure this layer ends. `VendoredTools` finds the prefix by walking up from the running binary looking for `tools.lock`, so a hostd copied out of the checkout correctly resolves nothing and falls through.

**hostd never provisions.** Nothing on the runtime path downloads, extracts or writes — it stats. A coding host must not reach the network because someone opened a panel.

**Not vendorable, and not attempted:** iOS simulator runtimes and `simctl` (inside Xcode, Apple's licence — `baguette` is the vendorable half), and the Android emulator with its system images (`sdkmanager`, licence-gated, gigabytes per API level — `adb` is the vendorable half). Both keep their existing host-discovery path and report unavailable when the host has none.

**Bumping a pin has a tail.** For `code-server` specifically: re-measure `CodePanelPresentation.clippedTitleBarHeight` against the new workbench (the title bar the client clips off — 35px on Code 1.112, 30px on 1.131; it is inline geometry, not a greppable CSS constant, so measure `getBoundingClientRect()` on `#workbench.parts.titlebar`), re-check `slopdesk-codeseed`'s `resources/settings.json` against the new settings schema (only registered keys may be seeded, and the shipped extension set moves between releases), and run `slopdesk-ops measure-code-server` — the daemon prewarms this child at boot (docs/DECISIONS 2026-08-07), so a spawn→listen regression lands on every hostd restart, not just on panel opens.

## Wire golden vectors — the long version

Manual binary encode (no JSON/Codable on the hot path); multi-byte ints big-endian; UUIDs 16 raw bytes. After wire changes run `slopdesk-gate golden`, then update `docs/20-wire-protocol.md`.

**Never** `>`-redirect the generator over `golden/golden_vectors.json` — it emits a subset (13 frozen keys are XCTest-only). Generate with **no** `SLOPDESK_*` env set. An intended format change is a surgical hand-merge, not a regeneration.
